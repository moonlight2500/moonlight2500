# 릴리즈 노트

이 프로젝트는 이 문서에서 처음으로 버전을 붙입니다(정식 출시 전이라 0.x 대). 버전 값은 `daytrader/__init__.py`
의 `__version__` 한 곳에서만 관리하고, 대시보드 상단과 `AutoDayTrading.exe --check`
출력에 그대로 노출됩니다.

## v0.30.0

전체 코드 점검(보안·매매 로직·UI) 결과를 반영한 대규모 업데이트입니다. 항목별 상세와 커밋 번호는 `docs/WORK_LOG.md` 에 있습니다.

### ★ 업데이트 전에 꼭 읽을 것
- **기록 저장 방식이 SQLite 로 바뀝니다.** 처음 실행할 때 기존 `state\*.jsonl`(거래·일지·주문 원장 등)을 `state\daytrader.db` 로 자동으로 옮깁니다. 원본은 지우지 않고 `.migrated` 로 이름만 바꿉니다. 옮기는 동안 잠깐 걸릴 수 있습니다.
- **새로 설치하면 비밀번호가 무작위 6자리로 만들어집니다**(기존 `123456` 폐지). 처음 실행할 때 콘솔·로그에 한 번 표시됩니다. 기존 `secrets.yaml` 을 쓰면 기존 비밀번호가 그대로 유지됩니다.
- **오버나잇 보유가 조건부로 바뀌었습니다.** 아래 "바꾼 것"을 확인하세요.
- 실거래 전에 반드시 모의매매(paper)로 먼저 돌려 보세요. 주문 처리 로직을 여러 곳 고쳤습니다.

### 추가한 것
- **새 화면 구성** - "심플&모던 스타일, 많은 스크롤 없이 편리한 네비게이션" 요청. PC 는 왼쪽 사이드바에서 16개 화면에 바로 가고(아이콘 줄로 접기 가능), 모바일은 하단 탭바(매매/기록/개선/설정)와 칩 메뉴로 오갑니다. 각 화면은 핵심 숫자 카드를 먼저 보여주고 긴 내용은 탭·접기로 정리했습니다. 설정은 "섹션 목록 + 한 섹션씩 보기"이고 해외주식·암호화폐·스윙 탭에도 섹션 메뉴가 생겼습니다.
- **조건부 오버나잇** - 장 마감 때 비용을 뺀 수익이 +2% 이상인 종목만 하루 넘기고, 손절선을 본전으로 올려 서버 손절을 다시 겁니다. 금요일·휴장 전날은 넘기지 않고, 넘긴 종목은 다음 날 장 마감에 무조건 청산합니다. 다음 날 서버 손절 재등록이 실패하면 텔레그램으로 알립니다.
- **SQLite 기록 저장소** - "로그가 많이 쌓여도 빠르게" 요청. 거래 원장·손익 곡선·판단 일지·주문 원장·시장 평가·백테스트 기록·경고 이상 로그를 `state\daytrader.db` 한 파일에 모았습니다(파이썬 기본 내장이라 추가 설치 없음). 로그는 90일 보관 후 자동 정리합니다. 백업은 `python run.py db-backup` 으로 실행 중에도 안전하게 할 수 있습니다.
- **세션별 백테스트 연구 도구** (`research/`) - 실제 매매 기법 코드로 1분봉을 재생해 기법×세션별 성과를 계산하고, 앞 70% 기간으로 설정을 고르고 뒤 30% 기간으로 검증합니다.
- **자동 테스트(CI)** - GitHub 에 올릴 때마다 윈도우·우분투에서 전체 테스트가 돕니다.

### 바꾼 것
- 사이드바 상단 상태 배지를 "대기" 대신 "자동매매 꺼짐/모의매매 중/실거래 중/매매 중단됨"으로 보여주고, 마우스를 올리거나(모바일은 탭) 누르면 뜻과 할 수 있는 일을 설명합니다.
- 상단 제목과 화면 안 제목이 겹쳐 나오던 것을 없앴습니다.
- 통합/국내주식/해외주식/암호화폐/스윙 탭의 글씨와 아이콘을 키우고, 이모지 대신 선 아이콘을 씁니다. 우측 상단 시장별 상태 표시도 선 아이콘과 상태 점으로 바꿨습니다.
- 설정 "거래선택"의 "국내주식 모드"를 "국내주식 사용"으로 바꿨습니다.
- ATR 을 Wilder 방식으로 계산합니다. 손절폭이 약 19% 넓어지므로 `atr_multiple` 재조정을 검토하세요.
- VWAP·당일 고가를 장 시작(09:00)부터 누적해서 계산합니다(이전에는 최근 80봉).
- 백테스트 체결을 다음 봉 시가 + 슬리피지로 바꾸고, 거래 20건 미만인 기법은 "최고 기법" 가산점을 받지 않습니다.
- AI 시황: 같은 입력이면 20분간 다시 부르지 않고, 기사 발행 시각과 작성 시각(KST)을 표시하며, 실패하면 원인(키 미등록/한도 초과/자료 없음)을 구분해 보여줍니다.
- 설정을 저장하기 직전에 `config.yaml.bak` 백업을 남깁니다.

### 고친 것
- **한 번에 최대 보유 종목 수보다 많이 사던 문제**(동시에 여러 후보가 통과할 때).
- **재시작하면 보유 종목의 서버 손절 때문에 실거래가 시작되지 않던 문제.**
- **해외주식·빗썸 이중 주문 위험** - 응답 시간 초과 뒤 실제로는 체결됐는데 다시 사던 문제. 국내와 같은 주문 원장·고유 주문 번호 방식으로 바꿨습니다.
- 서버 손절이 체결되는 순간 청산하면 "유령 포지션"이 남던 문제, 계좌 대조로 수량을 고쳐도 서버 손절 수량은 그대로이던 문제.
- 호가 올림 계산이 한 호가 낮게 나오던 문제, 현금 0원을 "값 없음"으로 보던 문제.
- 일일 손실 한도가 평가손실을 무시하던 문제, 강제청산이 마감 동시호가(15:20~)에 걸리는 설정을 허용하던 문제.
- 해외·코인·스윙의 "오늘"이 서버 시간대를 따르던 문제(한국시간으로 통일), 스윙 해외 종목 수량이 환율만큼 부풀던 문제.
- 느린 시세·뉴스 조회가 서버 전체를 멈추던 문제.
- 시장 탭에 파이썬 오류 원문(프록시·서버 주소)이 그대로 보이던 문제, 화면을 처음 열 때 1~2초 빈 화면이던 문제.
- `build_exe.bat` 을 실행하면 한글이 모두 깨지던 문제 - ANSI(CP949)로 다시 저장했습니다.

### 보안
- 로그인 실패 제한을 전체 합산으로도 걸고 재시작해도 유지합니다. 프록시 헤더(X-Forwarded-For)는 `DAYTRADER_TRUSTED_PROXY=1` 일 때만 믿습니다.
- 로그아웃하면 서버에서 세션을 무효화하고, "모든 기기에서 로그아웃"을 추가했습니다.
- 다른 출처(포트 포함)에서 오는 상태 변경 요청을 막고, 설정 경로(state_dir 등)는 프로그램 폴더 안만 허용합니다.
- 화면에 서버·외부 데이터를 넣는 곳 약 15곳을 이스케이프하고, 뉴스 제목을 통한 AI 프롬프트 조작을 막는 문구를 넣었습니다.
- 텔레그램 테스트에 임의 토큰을 쓰려면 비밀번호를 다시 확인하고, RSS 는 안전한 파서(defusedxml)로 읽고, 관리자 토큰은 주소창에 남지 않게 했습니다.

---

## v0.29.0

### 추가한 것
- **시장 평가 - 실제 지수·주가 기반 판단** - "장마감 정리를 단순 주요 뉴스 제목을 모으는 식으로 하지말고 워딩을 이해해서 직접 지수나 주가를 보고 판단해서 정리해" 요청. 뉴스 제목만 보고 판단하던 방식에서, 실제 지수 대용 ETF(SPY·QQQ·DIA)와 AI반도체 종목(엔비디아·AMD·브로드컴·TSMC·마이크론 등)의 실제 등락률을 함께 Groq 에 주고, 숫자를 우선 근거로 삼아 뉴스는 배경 설명으로만 쓰도록 바꿨습니다.
- **해외 시장 평가 - 신뢰도 높은 영어권 소스, 기술주·AI 반도체 집중** - "미장은 신뢰성 있는 해외 소식 위주로 취합하고 특히 기술주와 AI 반도체 관련 주식의 동향과 뉴스에 집중" 요청. 한국어로 재가공된 기사 대신 블룸버그 마켓·테크, 마켓워치 등 영어권 1차 소스를 새 "해외 기술주" 그룹으로 추가했습니다(전부 실제 응답을 확인한 뒤에만 등록). 해외 시장 평가는 국내 복기와 별개로 뉴욕장 마감 후(해외 복기 시각)에도 자동 발송되도록 확장했습니다.
- **시장 평가 기록 조회 화면** - "정리해서 보낸 내용을 프로그램에서 일자 시간별로 볼수 있게 추가해" 요청. [매매일지] 옆에 "시장 평가" 탭을 추가해 그동안 보낸 국내·해외 시장 평가를 날짜·시간순으로 다시 볼 수 있습니다.

### 고친 것
- 뉴스 제목의 아포스트로피(예: Investor's Business Daily)가 텔레그램 메시지에 `&#x27;`로 그대로 보이던 문제 - 불필요한 따옴표 이스케이프를 껐습니다.

---

## v0.28.0

### 추가한 것
- **오늘의 시장 평가(AI 요약) 텔레그램 알림** - "본장이 끝나고 시장에 대한 평가를 다른 투자사나 증권사 데이터나 외부 기관의 데이터를 참고해서 정리해 텔레그램으로 보내는 기능을 추가해. 이런 정보를 모으고 정리하는건 groq을 사용해" 요청. 내 매매 실적 복기(기존 기능)와는 별개로, 본장 마감 10분 뒤(기본 15:40) 그날 국내 증시 관련 뉴스(증권사 리포트·수급 동향 등, 기존 속보 소스 재사용)를 모아 Groq 로 3~5줄 요약해 보냅니다. 뉴스 제목·출처만 근거로 삼고(원문을 그대로 퍼오지 않음), 특정 증권사·기관 코멘트가 있으면 출처를 밝히도록 했습니다. 기본은 꺼짐이며 [설정] → 알림에서 켤 수 있고, Groq 키가 등록되어 있어야 실제로 보내집니다(없으면 조용히 건너뜀). 실제 배포 환경에서 뉴스 수집→Groq 요약→메시지 구성까지 전 과정을 직접 확인했습니다.

---

## v0.27.0

### 추가한 것
- **성과 화면 - 종목별 매매 집계** - "종목별로 총매수금액 총 매도금액 수수료 실현이익을 알수있게" 요청. "거래내역" 옆에 "종목별" 탭을 추가해 종목마다 거래 건수·총매수금액·총매도금액·수수료(추정)·실현이익·수익률을 한 줄로 볼 수 있습니다. 수수료는 거래 기록에 따로 안 남아 있어 "세전 손익(청산가-진입가)×수량"과 실제 손익의 차이로 계산합니다(수수료·세금·ETF 운용비 등 그 시장이 떼는 모든 비용을 포함, 시장마다 계산식이 달라도 항상 정확).
- **성과 화면 - 총 투입금액·수익률** - "총 얼마가 투입되서 실현이익이 몇프로인지" 요청. 상단 요약에 총 매수금액과 그 대비 손익률(수익률)을 추가했습니다.

### 고친 것
- **매매일지 세션 필터 - 통합·스윙에서 불필요하게 표시됨** - "매매일지 통합과 스윙은 세션이 필요없음" 지적. 통합은 시장마다 세션 이름·시간대가 달라 하나로 못 묶고, 스윙은 며칠~몇 주 보유라 세션 개념 자체가 없습니다. 두 경우 모두 세션 필터를 숨깁니다.
- **성과에서 매매기법을 클릭해도 설명이 안 나옴** - 암호화폐·해외주식은 분할 매도로 끝난 거래의 청산 사유가 "momentum_fade (분할 매도 2회 후)"처럼 설명이 덧붙어 있어, 기법 배지의 값이 실제 기법 키와 달라 클릭해도 반응이 없었습니다. 덧붙은 설명을 떼어 배지에는 깨끗한 기법 키만 쓰도록 고쳤습니다.
- **상단 달러/원화 전환 버튼 위치·모양** - "타원형 박스를 없애고 맨왼쪽 그 옆으로 다크모드 연결 버전 순으로 배치" 요청. 알약형 배경을 없애 다크모드 버튼과 같은 맨 아이콘 스타일로 바꾸고, 순서를 통화 전환 → 다크모드 → 연결 상태 → 버전으로 정렬했습니다.

---

## v0.26.0

### 고친 것
- **설정 메뉴에서 새로고침하면 비밀번호를 또 물어봄** - 재확인 토큰이 메모리에만 있어 새로고침 한 번에 사라졌던 게 원인. sessionStorage(탭을 닫으면 지워짐 - 새 방문자는 여전히 다시 물어봄)에 함께 저장해 같은 탭에서 새로고침해도 다시 안 물어봅니다.
- **암호화폐 설정을 바꿨는데 "스윙 거래 중"이라며 저장이 막힘** - 설정 저장 시 잠긴 시장의 값을 빼는 로직에서 스윙만 빠져 있었습니다. 설정 저장은 항상 전체 설정을 통째로 보내는 구조라, 스윙을 전혀 안 건드려도 스윙이 거래 중이면 저장 전체가(예: 암호화폐만 바꿨어도) 거부됐습니다. 국내·해외·암호화폐와 같은 방식으로 스윙도 잠겨 있으면 그 부분만 빼고 저장하도록 고쳤습니다.
- **거래선택 개요에 스윙 투자금액이 빠져있음** - "거래할 시장"에는 스윙 켜기/끄기가 있는데 바로 아래 "투자금액·동시 보유" 개요에는 국내·해외·암호화폐만 있고 스윙이 없었습니다. 스윙 투자금액·동시 보유 종목 수를 추가했습니다.
- **화면이 자동 갱신될 때마다 맨 위로 스크롤이 튐** - 종목선정·시장·실험실·대시보드·매매 시작/중지 카드가 몇 초마다 통째로 다시 그려지면서 보고 있던 위치를 잃고 맨 위로 튀었습니다. 다시 그리기 전후로 스크롤 위치를 저장·복원하도록 고쳐, 데이터는 갱신되어도 보던 위치 그대로 유지됩니다.

---

## v0.25.0

### 추가한 것
- **일일 최대 거래 횟수 - 암호화폐·해외주식** - "일 최대 거래건수는 아예 보이지도 않고" 지적으로 발견. 국내주식(위험 관리)에만 있던 과매매 방지 상한이 암호화폐·해외주식에는 아예 없었습니다. 암호화폐는 직전 24시간 롤링 기준, 해외주식은 국내주식과 별도의 독립된 값으로 각각 추가했고, 기본값은 국내·해외주식 모두 50회로 맞췄습니다(국내주식도 6회→50회로 올림). [설정] → 각 시장 화면에서 조정할 수 있습니다. 스윙은 "단타가 아니다"라는 판단에 따라 대상에서 뺐습니다(신규 진입 판정 자체가 하루 한 번뿐이라 일일 상한이 의미가 없습니다).

---

## v0.24.0

### 고친 것
- **momentum_fade 청산이 너무 자주·너무 이르게 발동** - "momentum_fade 가 계속 발생하는데 문제가 뭐야?" 질문으로 발견. 실제 해외주식 페이퍼매매 기록을 보니 청산 21건 중 17건(81%)이 momentum_fade였고 승률 35%·평균 손익 마이너스, 진입 4분 만에 잘린 거래도 있었습니다. 원인 둘을 고쳤습니다 - ① "거래량 위축" 판정이 봉 하나 대 봉 하나 비교라 진입 직후 거의 항상 통과 상태였던 것을 최근 3봉 평균으로 완화, ② 진입 직후부터 곧바로 평가되던 것에 최소 보유시간(10분) 유예를 추가했습니다(다른 청산 기법은 영향 없음). 문턱값도 다소 보수적으로 조정했습니다(연속 하락 4→5봉, 거래량 위축 기준 0.35→0.3배).

---

## v0.23.3

### 고친 것
- **모바일에서 숫자가 잘리거나 다음 줄로 밀림** - "계속 숫자 뒤에가 짤리거나 단위가 아랫줄로 나와. 한줄에 표현되게 수정" 요청. 예전 달러/원화 동시 표기 기능이 쓰던 "줄바꿈해 두 줄로 표시"용 CSS가, 그 기능이 달러/원 토글로 바뀌면서도 그대로 남아있던 게 원인이었습니다(대시보드 카드·매매일지·성과·보유종목 표 등 여러 곳). 이제 숫자는 항상 한 줄로 표시되도록 정리했고, 표 안의 숫자 칸은 말줄임 대상에서 빼 잘리지 않게 했습니다(폭이 부족하면 표를 옆으로 끌어 볼 수 있습니다). 대시보드 카드의 숫자는 화면이 좁을수록 글자 크기를 자동으로 줄여 최대한 한 줄에 들어오게 했습니다.

---

## v0.23.2

### 고친 것
- **원화 환산 표기 정리** - "원화로 환산할때 약 은 제외해" 요청으로 "약" 접두어를 뺐습니다. "단위가 천원으로 보여주는건 모바일만"이었다가 곧바로 "아니다 모바일도 원단위로 보여줘"로 정정돼, 최종적으로 모바일·PC 구분 없이 항상 전체 자릿수 원화("1,728,000원")로 통일했습니다.

---

## v0.23.1

### 고친 것
- **상단 달러/원화 전환 버튼이 잘 안 보임** - 다크모드 버튼(🌙)은 이모지라 배경 없이도 또렷했지만, 통화 전환 버튼("$")은 가는 글자 하나뿐이라 상단 띠 배경 위에서 흐리게 묻혔습니다. 반투명 알약 모양 배경·테두리를 줘서 버튼임을 뚜렷이 알아볼 수 있게 했습니다.

---

## v0.23.0

### 고친 것
- **해외주식 달러·원화 동시 표기가 산만함** - "달러와 원화를 같이 표현하니 산만하네. 원화 선택해서 볼수있게해" 요청. 달러 옆에 항상 원화를 덧붙이던 방식을 없애고, 상단에 $ / ₩ 전환 버튼을 추가해 달러 전용 또는 원화 전용(천원 단위) 중 하나만 골라 보도록 바꿨습니다. 기본값은 예전과 같은 달러 표시입니다. "해외주식이 포함된 곳에만 기능이 있으면돼" 요청에 따라 이 버튼은 해외주식 금액이 실제로 보이는 화면(대시보드·종목선정·성과·매매일지의 시장 필터가 "통합"·"해외주식"일 때, 계좌 현황)에서만 나타나고, 국내주식·암호화폐 전용 화면에서는 숨겨집니다.

---

## v0.22.0

### 고친 것
- **해외주식 원화 환산 표시가 모바일에서 잘림** - "모바일에서 원화표기가 잘 안보여" 지적으로 발견. 달러 금액 옆 괄호 안에 전체 자릿수 원화를 욱여넣던 방식이 좁은 화면에서 말줄임(…)으로 잘려 아예 안 보였습니다(모바일 전용 CSS가 원인). 이제 ① 원화를 천원 단위로 줄이고("1,728,000원" → "약 1,728천원") ② 달러 금액 아래 별도 줄에 작은 글씨로 표시합니다. "일반웹에서도 개선되어야" 요청에 따라 모바일 전용이 아니라 데스크톱을 포함한 모든 화면 크기에 동일하게 적용됩니다.
- **연속 손절 후 재개까지 너무 오래 걸림** - "다음 진입시기가 너무 늦어" 지적. 국내·해외주식·암호화폐의 재개 쿨다운을 24시간/6시간에서 모든 시장 공통 3시간으로 줄였고, 국내주식에도 해외주식처럼 "세션(프리장·본장·NXT장)이 바뀌면 3시간을 기다리지 않고 즉시 재개" 로직을 추가했습니다(해외주식은 이미 있던 기능). 스윙은 예전에 "일" 단위(기본 3일)로 재던 것을 다른 시장과 같은 "시간" 단위(기본 3시간)로 통일했습니다.

---

## v0.21.0

### 추가한 것
- **해외주식 원화 환산 표시** - "외화의 경우 현재 환율로 계상해서 원화로도 보여줘" 요청. 해외주식 진입가·현재가·손익 등 달러 금액 옆에 현재 환율 기준 원화 환산액을 괄호로 함께 표시합니다(대시보드·포지션·매매일지·실적 요약 전부). 환율은 실시간 조회에 실패하면 안전하게 기본값(1,400원)으로 대체됩니다.
- **[설정] "자동 설정" 기본 펼침** - "스크롤하지않고 쉽게 내용을 확인할 수 있게 변경해" 요청. 항목별로 접혀 있던 "자동 설정"을 기본으로 펼쳐 두어 클릭 없이 바로 세부 값을 볼 수 있습니다(v0.20.0에서 추가한 섹션 빠른 이동 칩으로 위치 찾기는 여전히 쉽습니다).

### 고친 것
- **해외주식 관심종목 보존 오류** - "관심종목은 전에 있던건 다 추가해 놔야지" 지적으로 발견. 시가총액 상위 확장(BRK.B·SPY·SMH·SOXX 추가) 작업 시 코드 기본값만 바꾸고 실제 배포된 설정 파일은 갱신하지 않아 반영되지 않았던 문제를 고쳐, 기존 15개 종목에 신규 4개를 더한 19개 전체가 보존되도록 했습니다.
- **해외 ETF 손익에 운용보수 반영** - "etf는 수수료이외에 운용비도 감안해야한다" 요청. SPY·SMH·SOXX는 매도 시 보유 기간에 비례한 연간 운용보수를 손익에서 차감합니다.

---

## v0.20.0

### 추가한 것
- **[설정] 화면 섹션 빠른 이동** - "항목별 스크롤 해야하니 네비게이션이 불편해" 요청. 섹션이 3개 이상인 그룹(예: 국내주식) 위에 섹션 제목 칩을 늘어놓고, 누르면 그 카드로 바로 스크롤합니다. 지금 보고 있는 섹션의 칩이 자동으로 강조됩니다.
- **관심 종목 검색해서 추가** - "관심주를 텍스트가 아닌 검색해서 목록에 추가하도록" 요청. 국내(테마 사전 기반)·해외(주요 종목 목록)·암호화폐(빗썸 실시간 마켓 목록) 모두 검색으로 찾아 "종목명(코드)" 형식으로 추가합니다. 코드만 덜렁 보이는 곳이 없도록 목록도 전부 이 형식으로 표시합니다.
- **암호화폐 거래대금 상위 10개 자동 감시** - "관심종목에는 시장별로 시가총액 상위 10개로 변경해" 요청 중 암호화폐 몫. 실시간 시가총액 API는 없어 기존에 있던 거래대금 기준 자동 감시 기능(`auto_top_volume`)을 기본값으로 켰습니다.
- **해외주식 기본 관심종목 확장** - 시가총액 상위 10개 + AI반도체 관련주 + 지수·반도체 ETF(SPY·SMH·SOXX)로 넓혔습니다.
- **숫자 입력에 천 단위 콤마** - "세자리마다 ,를 찍도록" 요청. 투자금액 등 큰 숫자를 다루는 설정 입력창에 입력 중에도 콤마가 붙어 자릿수를 한눈에 확인할 수 있습니다.
- **매매일지 세션별 필터** - "매매실적 조회시 장 세션별로도 조회할수 있게해" 요청. 시장 필터 옆에 세션 필터(국내: 프리장/본장/NXT장, 해외: 프리장/본장/애프터장/데이장)를 추가해 거래 건을 세션별로 걸러볼 수 있습니다(암호화폐는 세션 개념이 없어 제외).
- **해외주식 세션별 매매 선택**(v0.19.0에서 추가된 기능 보완) - "해외장도 본장이 디폴트 선택되도록" 요청에 따라 기본값을 국내주식과 같은 원칙(정규장만 켜짐)으로 바꿨습니다. 예전 24시간 기본값을 쓰던 분은 [설정]에서 다시 켜야 합니다.
- **속보 소스 확충** - "속보의 소스는 좀더 넓혀" 요청. 연합뉴스·한국경제(추가 카테고리)·매일경제(추가 카테고리)·코인텔레그래프(암호화폐 전용, 새 카테고리)를 추가하고, 마이클 버리·ARK·워런 버핏·헤지펀드 13F 포지션 공개 등 주요 투자자 동향 검색 소스 4개를 새로 추가해 총 9개→17개로 넓혔습니다. "속보는 증권·주식시장 관련만" 요청에 따라, 언론사 전체 RSS(증시와 무관한 기사도 섞이는 소스)는 제목에 증시 관련 키워드가 있어야만 통과시키는 필터를 추가했습니다(구글뉴스 검색 소스는 검색어 자체가 이미 증시로 좁혀져 있어 필터를 적용하지 않습니다).

### 고친 것
- **[설정] 화면 메뉴 배치 오류 재점검** - 국내주식 관련 설정이 엉뚱한 그룹에 있는지 전체 스캔했고, 이전 버전(v0.19.1)에서 이미 발견해 옮긴 것 외 추가 문제는 없음을 확인했습니다.

---

## v0.19.1

### 고친 것
- **[설정] 화면 메뉴 배치 오류** - "국내주식에 관련된거니 국내주식 메뉴에 들어가야지"라는 지적으로 발견. 새로 추가한 "국내주식 거래 세션"(프리장/본장/NXT장) 섹션이 국내주식 전용인데도 "거래선택" 그룹에 있어서 [국내주식] 메뉴에서 안 보였습니다. 점검하는 김에 전체를 스캔해 보니 "위험 관리"(손절·익절·연속 손절 한도 등 16개 항목, 전부 국내주식 전용) 섹션도 예전부터 "공통" 그룹에 잘못 들어가 있었습니다 - 둘 다 [국내주식] 메뉴로 옮겼습니다.

---

## v0.19.0

### 추가한 것
- **★★★ 국내·해외주식 세션별 매매 선택** - "본장·프리장·애프터장·데이장 등 장 별로 거래를 할지 사용자가 선택하게 해"(해외), "국장도 8시부터 9시까지 프리장, 9시부터 3시반까지 본장, 이후 NXT장"(국내) 요청. [설정] → 거래선택에서 시장별로 세션을 따로 켜고 끌 수 있습니다.
  - **해외주식**: 프리장(뉴욕 04:00~09:30)·본장(09:30~16:00)·애프터장(16:00~20:00)·데이장/야간거래(20:00~04:00) 4개 세션. 기존 "24시간 거래" 스위치를 대체합니다(전부 기본 켜짐 - 예전과 동일하게 동작).
  - **국내주식**: 프리장(08:00~09:00)·본장(09:00~15:30)·NXT장(15:30~20:00) 3개 세션. 프리장·NXT장은 기본 꺼짐(예전엔 이 시간대 매수 자체가 없었음) - 켜려면 [설정] → 매매 시간대에서 진입 시작·종료 시각도 해당 세션을 포함하도록 넓혀야 합니다.
  - 어느 쪽이든 세션과 무관하게 청산(보유 종목 관리)은 항상 계속됩니다 - 신규 매수만 막습니다.
- **암호화폐 저유동성 코인 매수 방지** - "암호화폐는 거래량이 적은 경우 거래를 하지 않도록 보완해" 요청. [설정] → 암호화폐에 "최소 거래대금(원)"을 추가했습니다(기본 500만원, 최근 10개 봉 평균). 유동성이 얇으면 호가 간격이 넓어 슬리피지가 크고 소량 체결에도 가격이 쉽게 튀는 문제를 막습니다. 0으로 두면 끕니다.

### 고친 것
- **해외주식 연속 손절 중단, 장이 바뀌면 즉시 재개** - 세션별 매매 선택 작업과 함께, 연속 손절로 신규 진입이 막혀도 마지막 손절이 일어난 세션과 지금 세션이 다르면 설정한 시간(기본 24시간)이 안 지났어도 즉시 재개하도록 했습니다(v0.18.3에서 먼저 추가, 이번 세션 구조 정리와 함께 재검증).

---

## v0.18.3

### 고친 것
- **★★★ 해외주식 진입가가 이상하게 찍히는 문제** - "해외주식 진입가가 이상해" 신고로 발견. 진입가는 분봉 종가(`/candles`) 하나만 보고 정하는데, 보유 종목 관리·청산은 실시간 시세(`/prices`)를 쓴다. 애프터마켓처럼 거래량이 얇은 시간대엔 분봉 하나가 단일 이상 체결(유동성 부족)로 실제 가격과 크게 어긋날 수 있다 - 실제로 AAPL이 진짜 가격($336)의 약 77배인 $25,988로 진입가가 기록된 사례가 있었습니다(그 시점 `/candles` 응답 자체가 그렇게 왔습니다). 매수 직전에 청산과 같은 실시간 시세로 한 번 더 교차 검증해, 5% 넘게 벗어나면 매수를 보류하도록 했습니다.

### 추가한 것
- **해외주식 연속 손절 중단, 장이 바뀌면 즉시 재개** - "프리장·본장·애프터장·데이장 등 구분된 장에서 연속 손절해도 장이 바뀌면 재개하도록 해" 요청. 예전엔 연속 손절로 신규 진입이 막히면 설정한 시간(기본 24시간)이 지나야만 풀렸습니다. 이제 시간이 안 지났어도, 마지막 손절이 일어난 세션(프리마켓/정규장/애프터마켓/데이장·야간거래)과 지금 세션이 다르면 즉시 재개합니다 - 세션마다 유동성·변동성 성격이 달라, 한 세션의 손절이 다음 세션의 기회까지 막을 이유는 없습니다.

---

## v0.18.2

### 고친 것
- **★★★ 휴장일(추석 등)에도 종목 선정을 계속 시도하던 문제** - "오늘은 휴일이라서 국장이 닫혀있는데 왜 자꾸 종목선정을 하려하지?"라는 신고로 발견. 휴장일 판정(`daytrader/session.py`)이 실제 토스 Open API의 `/market-calendar/KR` 응답 모양을 잘못 알고 있었습니다 - 코드는 `{"open": true/false}` 형태를 기대했지만, 실제 응답은 `{"today": {"integrated": null(휴장) 또는 정규장 시각 객체(거래일)}}` 형태였습니다. 그래서 `cal.get("open", True)`가 토스 응답에서는 항상 기본값(True)만 돌려줘, **어떤 휴장일이든 실제로는 절대 휴장으로 판정되지 않았습니다**(2026-09-24 추석 연휴에 직접 실시간 API를 호출해 확인). 이제 두 응답 모양(진짜 토스 API / 인터넷 폴백)을 모두 알아보고 올바르게 판정합니다. 재발 방지로 `tests/test_session.py`를 새로 추가했습니다.

---

## v0.18.1

### 추가한 것
- **다크모드** - 화면 상단 밴드에 🌙/☀️ 버튼을 추가했습니다. 기본은 OS 설정(prefers-color-scheme)을 따르고, 버튼을 누르면 직접 선택할 수 있으며 선택은 다음에 열 때도 유지됩니다(로그인 화면에서도 적용).

### 고친 것
- **다크모드 적용 중 발견한 대비 문제** - 배경이 `var(--ink)`인 버튼(기본 버튼, 토스트 알림, 탭 선택 표시)이 흰 글자를 고정으로 쓰고 있어서, 다크모드에서 `--ink`가 밝은 색으로 바뀌면 밝은 배경에 흰 글자가 남아 거의 안 보였습니다. 새 토큰(`--ink-contrast`)을 추가해 라이트/다크 각각에 맞는 글자색이 따라가게 했습니다.

---

## v0.18.0

### 추가한 것
- **★★★ 단타 매매 속도를 시장별로 분리** - "단타 매매 모드를 시장별로 분리해" 요청. 예전엔 매매 속도(보통/빠른 단타/스캘핑)가 `style` 값 하나로 국내·해외·암호화폐 전부에 동시 적용됐습니다. 이제 [설정] → 거래선택에서 국내주식·해외주식·암호화폐 각각 독립적으로 속도를 고를 수 있습니다(예: 국내만 빠른 단타, 나머지는 보통). 스윙은 원래 며칠~몇 주 보유가 전제라 이 개념이 없습니다.
  - **구조적 한계**: 해외주식은 국내주식과 진입 기법 목록을 공유합니다(Playbook 설계상). 그래서 "빠른 단타 때 진입 기법을 더 추가"하는 동작은 국내 속도로만 판단하고, 해외 속도는 손절·익절·시세 확인 주기에만 적용됩니다.
  - 이 김에 해외주식의 스캘핑(scalp) 값이 원래 아예 없던 것도(예전엔 국내·암호화폐만 있었음) 채워 넣어 일관성을 맞췄습니다.
- **⚠ 설정 구조 변경**: `style`이 최상위 항목에서 `risk.style`/`overseas.style`/`crypto.style`로 이동했습니다. 기존 config.yaml의 최상위 `style` 값은 더 이상 읽히지 않고, 각 시장이 개별 기본값(normal)으로 시작합니다 - 필요하면 [설정] 화면에서 다시 골라 주세요.

### 검증
- 보안 점검(사용자 확인 요청) - 로그인 비밀번호·API 키(secrets.yaml)는 Windows DPAPI로 암호화 저장되어 있고, 신규기기 승인 화면(/admin)은 같은 PC(루프백)에서 리버스 프록시를 거치지 않은 요청 + 매 실행마다 새로 만드는 1회성 토큰까지 이중으로 확인해야만 열립니다 - 둘 다 기존에 이미 견고하게 구현되어 있음을 코드로 확인했습니다.

---

## v0.17.5

### 추가한 것
- **암호화폐 일일 복기 하루 2회** - [설정] → 알림에 "암호화폐 복기 시각 2(선택)"를 추가했습니다. 비워두면 예전처럼 하루 한 번(기본 21:00)만 보냅니다.
- **주간·월간 복기(전체 시장 통합)** - "전체 주, 월 보내라"는 요청. [설정] → 알림에서 켜면, 국내·해외·암호화폐·스윙 4개 시장을 한 메시지에 묶어 지정한 요일/날짜·시각에 지난 7일(주간) 또는 지난 달 전체(월간)를 복기해 보냅니다. 통화 단위가 시장마다 달라(원/달러) 하나의 숫자로 합산하지 않고 시장별로 줄을 나눠 보여줍니다. 기본은 꺼짐(원하는 사람만 켜도록).

### 안전장치
- **`build_exe.bat` 백업 유실 버그** - 빌드가 중간에 실패해 `dist\AutoDayTrading`이 이미 비어 있는 채로 스크립트를 다시 돌리면, 멀쩡한 옛 백업이 빈 백업으로 덮어써져 API 키·거래 기록이 사라질 수 있었습니다(2026-09-24 실제로 발생). 이제는 `dist\AutoDayTrading`에 `secrets.yaml`이나 `state\`가 실제로 있을 때만 백업을 새로 만들고, 없으면 기존 백업을 건드리지 않습니다.

---

## v0.17.4

### 고친 것
- **[실험실] 종목별 매매기법 백테스트 결과가 안 보이던 문제** - "테마별 평균"은 기법 결과를 전부 한 줄 텍스트로 이어 붙여서(예: "기법A(+1%, 3종목) · 기법B(-2%, 1종목) · ...") 기법이 많으면 줄이 너무 길어 비교가 힘들었고, [실험실]의 "최근 실행 기록"표의 "결과" 칸도 종목이 여러 개면 쉼표로 이어진 한 줄이라 구분이 힘들었습니다. 둘 다 종목별 표(테마별 평균) 또는 줄바꿈되는 조각(실행 기록)으로 바꾸고, 손익도 색으로 구분했습니다(이익 빨강/손실 파랑).

---

## v0.17.3

### 고친 것
- **[매매 기법] 화면에서 "새 기능이 안 보인다"는 신고** - 원인은 표(가로 스크롤 표) 안의 ⓘ 설명 아이콘을 누르면 뜨는 설명 상자가 표의 스크롤 영역 밖으로 밀려나 화면 밖에 그려지고 있었던 것입니다(누르면 뜨긴 뜨는데 잘려서 안 보임). 화면 좌표 기준으로 위치를 다시 계산해 뷰포트 안에 항상 보이게 고쳤습니다.
- **진입 기법·청산 기법 목록이 기본적으로 접혀 있던 것** - "펼치기"를 눌러야만 뭐가 켜져 있는지 보였습니다. 이제 기본으로 펼쳐서 보여주고, 필요하면 접을 수 있습니다.

---

## v0.17.2

### 고친 것
- **★★★ 해외주식·암호화폐·스윙의 "실적 가산점"이 항상 중립(1.0)이었던 문제** - v0.17.1 의 매매 모델(`technique_learning_mode`) 조사 중 발견. 진입 기법에 실전 승률 가산점(최대 ±30%)을 주는 로직(`Playbook.set_performance()`)을 국내주식 엔진만 호출하고 있었습니다 - 해외주식·암호화폐·스윙은 `entry_pref`/`entry_exit_pref`로 설정해도 이 가산점만은 전혀 반영되지 않았습니다(다른 가산점인 [실험실] 종목별 백테스트 선호도는 4개 시장 모두 정상 작동하고 있었습니다). 세 엔진 모두 자기 청산 기록(`state.closed`)에서 기법별 승률을 계산해 채워주도록 고쳤습니다.

### 추가한 것
- **[실험실]에서 매매 모델·속도 비교 실험** - "이 시뮬레이션 로직을 실험실에 추가해달라"는 요청. [실험실] → 새 프리셋 5개(매매 모델 3종: 기본 룰만/진입 가산점/진입+청산 가산점, 속도 2종: 빠른 단타/스캘핑)를 최대 3개까지 골라 같은 시세로 동시에 가상 매매시켜 비교할 수 있습니다. 이 김에 실험실에서 속도(style)를 바꿀 때 손절·익절 폭이 실제로는 적용 안 되던 버그도 같이 고쳤습니다.
- **대시보드에 시장별 매매 모델·기법별 가산점 표시** - "대시보드에서 거래시장별로 어떤 모델이 돌아가는지", "각 매매기법별로 가산점이 어떻게 되어 있는지 시장별로 보고 싶다"는 두 요청. [매매 기법] 탭에 "시장별 모델·가산점" 서브탭을 추가해, 국내·해외·암호화폐·스윙 4개 시장 각각 지금 켜진 매매 모델과 진입 기법별 실제 가산점 배수·근거를 한 화면에서 확인할 수 있습니다.

---

## v0.17.1

### 고친 것 - v0.17.0 배포 후 실거래(모의매매)에서 확인된 문제
- **★ "최근 10시간 거래실적이 매우 안 좋다"** - v0.17.0 의 "익절폭을 넓혀 크게 먹기" 개편이 실제로는 전혀 작동하지 않고 있었습니다. 크립토·해외주식 32건 중 30건이 손실로, 전부 ATR 손절(atr_stop)·모멘텀 소멸(momentum_fade)로만 청산되고 고정 익절·트레일링은 단 한 번도 안 나왔습니다. 원인 두 가지를 고쳤습니다.
  - `atr_multiple`(ATR 기반 손절 배수)이 익절폭을 넓힐 때 같이 안 넓어지고 그대로 3.0배에 머물러 있었습니다(설정으로 노출된 적이 없어 늘 코드 기본값). **4.5배로 넓혔습니다**(`strategy.params.atr_stop.atr_multiple`).
  - `momentum_fade`의 임계값(거래량 위축 비율·연속 하락 봉수)도 코드 기본값 그대로인 채 해외주식에 새로 켜져 과발동했습니다. **완화했습니다**(연속하락 3→4봉, 거래량위축기준 0.5→0.35).
- **★ "빠른 단타에선 폭을 줄여야 하지 않나"** - `style: fast`(빠른 단타)가 v0.17.0 에서 "보통"과 같은 넓은 목표(국내 15%, 크립토 25%)를 그대로 쓰게 바꿨는데, 실제로는 수십 분짜리 빠른 단타 보유시간 안에 그 목표에 현실적으로 닿지 못해 전부 다른 청산으로만 잘렸습니다. **손절은 검증된 값(2.5%)을 유지하고 익절만 실제로 닿을 수 있는 크기(5%)로 다시 좁혔습니다** - 예전처럼 손절은 그대로 두고 익절만 좁히는 위험:보상 비율을 지키는 방식입니다.
- **★ "보통 속도의 목표도 스윙 수준 아닌가"** - "보통" 속도(fast 가 아닐 때)의 익절폭도 국내 15%·크립토 25%로, 스윙(40%)과 거의 구분이 안 될 만큼 넓었습니다. **8%로 낮춰 스윙과 뚜렷이 구분되게** 했습니다(스윙은 손대지 않음).
- **★ "크립토 목표도 주식하고 동일하게 세팅해"** - 암호화폐만 변동성이 크다는 이유로 국내·해외 주식보다 넓은 손절·익절·추적폭을 따로 쓰고 있었는데(보통 손절 6%/익절 12%, fast 3.5%/8%), 국내·해외 주식과 완전히 같은 값(보통 3.5%/8%, fast 2.5%/5%)으로 통일했습니다. 필요하면 [설정] 화면에서 암호화폐만 다시 넓힐 수 있습니다.
- **★ 진입+청산 가산점("entry_exit_pref") 모델의 휩쏘 오학습 버그** - 이번에 새로 만든 비교 시뮬레이션으로 찾았습니다. 잠깐 이익권에 올라갔다가 반전해 손실로 끝난 거래("휩쏘")까지 "이익권에 있었다"는 이유로 청산 효율 기록에 반영해, 휩쏘가 잦은 종목을 "너무 일찍 판다"고 잘못 배워 익절 목표를 넓혀 버렸습니다. 그러면 다음 휩쏘에서 더 오래 들고 있다가 더 크게 잃는 악순환이 됩니다. 이제는 **최종적으로 이익이었던 거래만** 청산 효율에 반영합니다(`daytrader/exit_efficiency.py`).

### 추가한 것
- **시장 상황에 따른 익절폭 자동 확대(ADX 기반)를 시장별로 켜고 끌 수 있게** - [설정] 화면에 국내·해외·암호화폐·스윙마다 "추세 강하면 익절폭 자동 확대" 체크박스를 추가했습니다(`risk/overseas/crypto/swing.dynamic_take_profit`).
- **★★★ 거래시장별 매매 모델 선택** - "거래시장별로 어떤 모델을 선택할지는 사용자가 선택"이라는 요청에 따라, [설정] 화면에 시장마다(국내·해외·암호화폐·스윙 각각 독립) "매매 모델" 선택 항목을 추가했습니다(`technique_learning_mode`). 세 모델은 실제로 이렇게 다르게 동작합니다:
  - **기본 단타 룰만("none")** - 켜둔 진입 기법들이 그 순간의 신호 강도만으로 경쟁합니다. 어떤 기법이 최근 잘 맞았든, 과거 승률이 얼마든 전혀 안 보고, 지금 이 순간 가장 강한 신호를 낸 기법이 무조건 이깁니다. [실험실] 백테스트는 계속 돌지만 결과가 실제 매매에 전혀 반영되지 않습니다(화면에서 보기만 가능).
  - **진입 가산점 반영("entry_pref", 기본값)** - 위와 같은 신호 강도 경쟁에, 두 가지 가산점을 곱해 순위를 조정합니다. ① [실험실]이 "이 종목/테마엔 이 기법이 최근 더 잘 맞았다"고 찾아둔 기법에 1.15배. ② 그 기법의 최근 실전(모의매매 포함) 승률이 50%보다 높으면 최대 1.3배, 낮으면 최대 0.7배(표본 20건 미만이면 반영 안 함). 신호가 약간 약해도 최근 실적이 좋았던 기법이 역전해서 뽑힐 수 있습니다.
  - **진입+청산 가산점 반영("entry_exit_pref")** - 위 진입 가산점에 더해, 청산 쪽에도 학습이 붙습니다. 보유 중 도달했던 최고가(가장 팔기 좋았을 때) 대비 실제로 청산에서 챙긴 이익의 비율을 종목마다 계속 기록해서(`daytrader/exit_efficiency.py`, 새 모듈), 어떤 종목에서 "번번이 너무 일찍 팔아 이익을 놓쳐왔다"는 게 표본 3건 이상 쌓이면, 다음에 그 종목에 다시 들어갈 때 익절 목표를 자동으로(최대 1.5배까지) 넓혀서 더 오래 들고 가게 만듭니다. 이미 효율적으로 챙겨온 종목은 아무 변화가 없습니다. [실험실]의 과거 시세 재생과는 완전히 별개로, 실제/모의매매 결과만 보고 학습합니다 - 그래서 이 모드는 매매를 어느 정도 실제로 해봐야(최소 각 종목당 3번 이상 청산) 효과가 나타나기 시작합니다.

---

## v0.17.0

### 매매원칙 개편 - "잃지 않는다"에서 "수익을 최대한 추구한다"로
- **★ "매매원칙을 조금 수정해. 잃지않는다가 아니라 수익을 최대한 추구한다로. 매매로직과 방향도 전면 수정해."** 네 시장(국내·해외·암호화폐·스윙) 모두에 적용되는 방향 전환입니다.
  - 베팅을 더 공격적으로: 신호 강도에 따른 매수 배수 폭을 넓히고(min_mult~max_mult), 추가 매수(피라미딩) 횟수를 2→3회로 늘렸습니다. 다만 첫 매수 비중을 낮춰(initial_ratio) 강한 신호일수록 첫 매수에서 한도를 다 써버려 정작 피라미딩을 못 쓰는 문제를 피했습니다(실측 테스트로 확인).
  - 손절·익절·추적손절 폭을 전반적으로 넓혔습니다(국내·해외·암호화폐·스윙 전부). 분할 매도는 초반에 적게만 챙기고(first_exit_ratio↓) 나머지 대부분은 트레일링 스탑에 맡겨 추세가 사는 한 계속 태웁니다.
  - **오버나이트·장기 보유를 허용**(`exit.allow_overnight`) - 장 마감이 지나도 손절·트레일링·시간손절 등 다른 청산 기법은 계속 감시하되, "무조건 오늘 안에 판다"는 규칙만 뺐습니다.
  - **정해진 익절폭 + 실시간 신호에 따른 동적 조정**: "정해진 익절폭으로 운영하되 실시간 시그널이 강할 때 폭을 넓히거나, 시그널에 따라 목표에 도달하지 못했어도 익절한다"는 요청에 따라 두 가지를 새로 연결했습니다. ① ADX(추세 세기)가 강할 때만 익절 목표를 넓히는 `dynamic_take_profit_pct()`를 국내·해외·암호화폐·스윙 전부에 연결(기준값은 [설정]→공통에서 조절 가능). ② 신호가 약해지면 목표 미달이어도 미리 정리하는 `momentum_fade` 청산 기법을 국내·해외·스윙에도 켬(기존엔 암호화폐 전용) - 스윙은 이 기법이 실제로 작동하도록 청산 관리 시점에 일봉을 새로 받아오게 바꿨습니다.
  - 진입 기법을 더 다양하게(국내 entry_order에 bull_flag·volume_dry_pop·ma_pullback 추가) - 단, 아직 모의매매로 검증 전인 vwap_reclaim은 기존 안전장치(신규 기법은 검증 전까지 기본 꺼둠)를 지켜 기본값에서 뺐습니다.
  - **해외주식 손절·익절·추적폭·최대보유시간을 국내와 분리**했습니다(`overseas.stop_loss_pct`/`take_profit_pct`/`trailing_pct`/`trailing_arm_pct`/`max_hold_minutes`, 기본값은 지금까지 공유해 온 국내 값과 동일) - 원화·달러, 변동성이 다른 시장인데 국내용 값을 그대로 썼던 것을 암호화폐처럼 독립시켰습니다. 종목별 백테스트([실험실])도 해외주식·암호화폐 각자의 값으로 채점하도록 함께 고쳤습니다(전엔 국내 기준으로 채점되고 있었습니다).
  - `style: fast`(빠른 단타)가 손절폭은 그대로 두면서도 익절폭만 조용히 좁히던(국내 4%→3.2%, 암호화폐 5%→4%) 옛 규칙을 없앴습니다 - "익절폭을 넓혀 크게 먹기" 개편을 fast가 무력화하는 문제였습니다. fast는 이제 시세 확인 주기·진입 기법 다양화만 담당하고, 목표 폭은 손대지 않습니다.

### 고친 것
- **★ "대시보드에서 현금 잔여금액이 안 맞어"** - 재시작할 때마다 모의매매 현금이 그동안의 손익과 무관하게 총 투자금액으로 초기화되는 버그가 암호화폐·해외주식·스윙 엔진에 있었습니다(국내주식은 이미 고쳐져 있었음). 지금까지 실현손익을 더하고 보유 포지션에 묶인 돈을 뺀 값으로 복원하도록 세 엔진 모두 맞췄습니다.
- **[매매원칙] 개편 중 발견한 시뮬레이션 무한 루프**: 오버나이트 허용으로 인해 sim/replay(백테스트) 모드에서 포지션이 장 마감 후에도 안 닫히면, "하루치만 재생하고 끝난다"는 시뮬레이션의 전제가 깨져 엔진이 멈추지 않는 버그가 있었습니다. sim/replay는 오버나이트 여부와 무관하게 항상 당일 강제청산하도록 고쳐 해결했습니다(실거래·모의매매는 그대로 오버나이트 유지).
- 스윙매매 대상을 국내·해외·암호화폐 세 시장 모두로 확장하면서 생긴 통합 대시보드 표시 문제를 고쳤습니다.
- 설정 화면의 거래선택 잠금을 항목 단위로 세분화하고, 스윙 청산 기법 기본값 누락을 고쳤습니다.
- 종목 선정 없음 안내 문구가 themes.yaml에 없는 종목을 "가장 많이 오른 종목"으로 잘못 보여주던 문제, "과다거래 의심" 오탐 배너를 없앴습니다.

### 추가한 것
- **[매매 기법] 화면에 "🖼 사례로 보기" 탭 추가**: "매매별로 로직을 정리해서 웹에서 볼 수 있도록 해줘, 사례별로 주가추이를 그림으로 표현해줘"라는 요청에 따라, 진입·청산 기법 22종 각각이 어떤 주가 흐름에서 발동하는지 예시 도식(점선=기준선, 점=매수·매도 지점)으로 보여줍니다. 같은 기법이 여러 시장에서 쓰이면 한 장으로 모으고 어느 시장에서 켜져 있는지 표시합니다.
- [매매 기법] 화면의 "자동 선정 로직" 설명에, [실험실] 백테스트로 종목별 선호 기법을 찾아 1.15배 가산점을 주는 로직(technique_prefs)을 새로 설명했습니다 - 이 가산점은 진입(매수)에만 적용되고 청산(매도)에는 적용되지 않습니다(청산은 손절이 항상 먼저인 고정 우선순위라, 가산점을 넣으면 손절보다 다른 청산이 먼저 발동할 위험이 있어 적용하지 않기로 결정).

---

## v0.16.2

### 고친 것
- **★ "당연히 같은 종목을 여러 번 할 수 있어. 이런 메시지는 불필요해."** - 대시보드의 "⚠ 과다거래 의심" 배너를 없앴습니다. 관심 종목이 적은 시장(암호화폐 등)에서는 같은 종목을 하루에 여러 번 사고파는 것 자체가 정상적인 동작인데, 이 배너는 그런 경우마다 오탐(false positive) 경고를 띄우고 있었습니다.
- **★ "오늘은 테마로 인정될 만큼 동반 상승한 종목 묶음이 없습니다. 오늘 가장 많이 오른 종목이 0010S0 +288.33%로, 상승 인정 기준(3%)에 못 미칩니다."** - 종목 선정이 없는 날 이유를 설명하는 문구가, themes.yaml에 등록되지 않은 종목(워런트·ELW 등 거래소 전체 등락률 순위에 잡히는 추정 종목)을 "가장 많이 오른 종목"으로 보여주면서 앞뒤가 안 맞는 말을 하고 있었습니다(기준(3%)보다 훨씬 큰 값을 보여주면서 "기준 미달"이라고 말함). 이제 "가장 많이 오른 종목"은 실제로 테마 후보가 될 수 있는(themes.yaml에 등록된) 종목 중에서만 찾고, 개별 종목이 기준을 넘겼는데도 테마로 인정 안 된 경우(같은 테마에서 함께 오른 종목 수가 부족한 경우)에는 "기준 미달"이 아니라 그 진짜 이유를 설명합니다.

---

## v0.16.1

### 고친 것
- **★ "설정에서 거래선택시 현재 매매중이 아닌 건 선택할 수 있어야 한다"** - [설정] 화면이 탭(그룹) 하나를 통째로 국내주식 거래 중 여부 하나로만 잠갔습니다. 그래서 국내주식만 거래 중이어도 아직 켜지 않은 스윙·해외주식·암호화폐의 "사용" 체크박스·투자금액까지 함께 잠겨 바꿀 수 없었습니다. 이제 항목 하나하나를 그 항목이 실제로 속한 시장이 거래 중일 때만 잠급니다(백엔드 저장 검증 기준과 동일) - 예를 들어 국내주식만 거래 중이면 스윙·해외주식·암호화폐 관련 항목은 그대로 켜고 끌 수 있습니다.
- **★ "스윙매매 선택 후 저장 시 매매기법이 없다고 나온다"** - config.yaml 에 `swing:` 섹션 자체가 없는(예: 이번에 새로 추가된 시장이라 기존 설정 파일엔 없음) 경우, 설정 화면이 진입·청산 기법 체크박스를 전부 해제된 상태로 보여주고 있었습니다(화면 데이터를 채우는 로직이 `entry_order`/`exit_enabled`처럼 `default_factory`로 정의된 기본값을 빠뜨렸습니다). 그 상태로 저장하면 "swing.entry_order 가 비어 있습니다"로 거부됐습니다. 이제 이런 기본값도 빠짐없이 채워서, 처음 스윙을 켤 때부터 기본 기법 3종이 이미 선택된 채로 보이고 그대로 저장됩니다.

---

## v0.16.0

### 추가한 것
- **스윙 매매 대상을 국내·해외주식·암호화폐 세 시장 모두로 확장**: "스윙매매 대상은 국내주식 해외주식 암호화폐 모두 해당되도록 해"라는 요청에 따라, 스윙은 이제 시장마다 따로 켜고 끄는 게 아니라 하나의 엔진이 세 시장 포지션을 함께 관리합니다(포지션마다 어느 시장 것인지 표시됩니다). 국내·해외는 같은 토스 계좌를, 암호화폐는 빗썸 계좌를 그대로 씁니다(빗썸 키가 없으면 암호화폐 레그만 건너뜁니다). 투자금액은 세 시장 통화를 환산 없이 그대로 섞어 쓰는 근사치입니다("통합" 화면이 원화·달러를 단순 합산하는 것과 같은 방식).
- **종목선정 기준에 "최근 1주 실제 등락" 추가**: "종목선정시 단타매매와는 다른 기법 사용하고, 최근 일주일 동안 실제 주가 변동에 따라 선정하도록 해"라는 요청에 따라, 기존 이동평균 추세 필터에 더해 최근 5거래일(약 1주일) 동안 종가가 실제로 얼마나 움직였는지(확정된 결과)를 확인해 최소 기준(`swing.week_momentum_min_pct`, 기본 0%=보합 이상) 이상 오른 종목만 후보로 남깁니다. 당일 실시간 등락률·거래량으로 고르는 단타와 뚜렷이 다른 선정 방식입니다.
- 시장마다 스윙 전용 관심 종목을 따로 둘 수 있습니다(`swing.watchlist`=국내, `swing.overseas_watchlist`=해외, `swing.crypto_watchlist`=암호화폐) - 각 시장의 단타 관심 종목과는 별개 목록입니다.
- 세 시장 후보를 한 시장이 독차지하지 않도록 번갈아(round-robin) 담아, 국내 후보가 많아도 해외·암호화폐 후보가 밀려나지 않게 했습니다.
- 같은 종목이 국내 단타와 스윙 양쪽의 대상이 될 수 있다는 점(선정 기법과 매매 기법이 다르므로)을 반영해, 통합 대시보드의 보유 종목 집계가 시장:종목 단위로 구분되도록 고쳤습니다(전에는 같은 종목코드를 쓰면 한쪽 포지션이 통합 화면에서 안 보일 수 있었습니다).

---

## v0.15.0

### 추가한 것
- **스윙 매매(며칠~몇 주 보유)를 4번째 시장으로 추가**: 국내 단타와 같은 계좌(TossClient)를 쓰지만, 봉 하나가 1분이 아니라 하루(일봉)인 완전히 다른 시간축으로 돕니다. "단타 외에 스윙 매매도 추가해달라, 기법도 종목선정도 달라야 한다"는 요청에 따라 세 가지 모두 단타와 독립입니다.
  - **전용 진입 기법 3종**(일봉 전용): 이동평균 눌림목(60일선 위·20일선 되돌림·전날 고가 재돌파), 박스권 돌파(20거래일 고가를 거래량 동반 돌파), 골든크로스(20일선이 60일선을 최근 며칠 안에 상향 돌파). 청산은 단타보다 훨씬 넓은 손절·익절·추적손절·최대 보유일수(기본 손절 8%·익절 20%·최대 20일)만 씁니다.
  - **전용 종목선정**: 국내 단타와 같은 테마 후보 풀 + 스윙 전용 관심 종목(`swing.watchlist`)을 합친 뒤, 설정한 이동평균(기본 60일) 위(중기 상승 추세)인 종목만 실제 후보로 남깁니다. 진입 판정은 매일 정규장이 끝난 뒤(그날 일봉이 확정된 뒤) 한 번만 하고, 보유 종목의 손절·추적손절 관리는 장중에도 계속합니다.
  - **실험실 연동**: 종목이 선정되면 다른 세 시장과 같은 원칙으로 [실험실] 백테스트가 자동으로 돌아 "이 종목엔 이 스윙 기법이 최근 더 잘 맞았다"는 결과를 진입 기법 채점에 반영합니다(`technique_backtest.py`가 스윙일 때는 일봉·약 8개월치로 백테스트합니다).
  - **화면**: 대시보드·종목선정·성과·매매일지·설정 전 화면에 "스윙" 탭이 추가되어 다른 세 시장과 같은 구성(상태 카드·보유·감시·손익·진행 로그)으로 보고 조작할 수 있습니다.
  - **제약**: 스윙 실거래(live)는 아직 지원하지 않습니다 - 재시작 시 계좌 잔고와 대조해 복구하는 안전장치(암호화폐·해외주식엔 있음)가 아직 없어, 관찰(web)·모의매매(paper)로 먼저 충분히 검증한 뒤 추가할 예정입니다.

---

## v0.14.0

### 추가한 것
- **종목별 매매기법 백테스트를 자동 실행**: 국내·해외·암호화폐 세 시장 모두, 종목이 새로 선정(재선정)될 때마다 [실험실]의 백테스트가 백그라운드로 자동 실행되어 결과를 저장합니다. 매매 루프를 막지 않도록 항상 별도 스레드에서 돌고, 실패해도 매매에 영향이 없습니다. 오늘 같은 종목 조합으로 이미 돌렸으면 다시 돌지 않습니다(재선정마다 API를 낭비하지 않음).
- **실행 기록 조회**: [실험실] 탭에 "최근 실행 기록" 카드를 추가해, 수동으로 실행한 것과 자동으로 돈 것을 시각·시장·계기(수동/자동)·종목별 1등 기법과 손익까지 최신순으로 볼 수 있습니다(`GET /api/lab/technique_backtest/history`).
- 해외주식 백테스트도 실제 엔진처럼 관심 종목(overseas.watchlist)을 테마와 별개로 항상 후보에 포함하도록 고쳤습니다 - 테마로 인정될 조합이 없는 조용한 시간에도 관심 종목으로 결과가 나옵니다.

### 고친 것
- **★ 해외주식 백테스트가 봉을 하나도 못 받아 항상 "0건"으로 끝났습니다.** 시세 API 가 한 번에 200개 넘게 요청하면 오류 없이 조용히 빈 배열을 주는데, 국내 종목은 우연히 다른 경로(네이버 폴백)로 빠져 이 한도에 안 걸렸을 뿐이었습니다. 200개씩 나눠 받도록 고치고, 실제 등록된 키로 애플(AAPL) 등 여러 종목에서 정상적으로 봉을 받는 것을 확인했습니다.

---

## v0.13.0

### 추가한 것
- **종목별·테마별 매매 기법 백테스트**([실험실] 탭): 오늘 선정된 테마 종목(국내·해외·암호화폐 중 선택)에 최근 실제 시세(1분봉, 기본 7일)를 기법별로 독립 재생해 손익을 비교하고 순위를 매깁니다. "종목별로 매매 기법이 달라져야 하는지"를 확인하기 위한 기능입니다. 청산은 모든 기법에 공통으로 지금 설정된 손절·익절·추적손절만 써서 진입 신호끼리 공정하게 비교합니다(ATR 손절 등 고급 청산·분할 매수·매도는 재현하지 않음). 테마 대장주처럼 같은 시각 다른 종목 시세가 함께 필요한 기법은 이 방식으로는 신호가 잘 안 나올 수 있습니다(한계로 명시).
- **백테스트 결과를 실전 매매에 자동 반영**: "결과를 실전 매매에 반영" 체크 후 실행하면, 종목(우선)·테마별로 가장 좋았던 기법이 저장되어(`state/technique_prefs.json`) 이후 모의·실거래의 진입 기법 채점에 가산점(1.15배)으로 들어갑니다. 강제 규칙이 아니라 가산점이라 신호가 없는 기법을 억지로 사지는 않습니다.
- **[소개] 페이지 추가**: 프로그램이 무엇인지 요약하고, 기능·제약사항/주의사항을 목록으로 정리했습니다([Improve] 그룹 맨 앞).

---

## v0.12.0

### 바꾼 것
- **국내 종목 선정은 이제 장이 열려 있는 시간에만 돕니다.** 예전엔 매수 가능 시간(정규장 등)이 지나도 자정까지, 그리고 새벽부터 개장 전까지도 종목 선정(랭킹 조회)이 계속 돌았습니다(개장 전 판정 구간이 자정부터였음). 이제 국내 시장이 프리마켓·정규장·NXT 어떤 형태로든 열려 있다고 볼 하루 구간(기본 08:00~21:00, `entry.selection_day_start`/`selection_day_end`로 조절 가능)에만 종목 선정을 돌리고, 그 밖(밤 9시~다음날 8시)과 주말·공휴일에는 돌리지 않습니다. **매수 가능 시간대(스캔 시작·종료, 장 초반·막판 시간대)는 전혀 바뀌지 않았습니다** - 선정을 더 자주/오래 계산할 뿐, 실제로 살 수 있는 시간은 그대로입니다.
- 해외주식(미국)은 원래도 평일 24시간, 휴장일만 제외하고 종목 선정이 계속 돌고 있었습니다 - 확인만 하고 바꾸지 않았습니다.

---

## v0.11.0

### 추가한 것
- **새 IP·기기 접속 승인**: 처음 보는 IP·기기 조합이 비밀번호까지 맞게 입력해도 곧바로 로그인시키지 않고 "승인 대기" 상태로 잡아 둡니다. 서버가 도는 PC의 **트레이 메뉴 → "새 기기 승인 관리"**에서만 열리는 관리자 페이지(`/admin`)에서 대기 중인 요청(IP·기기·요청 시각)을 승인/거부하고, 승인된 기기 목록도 확인·해제할 수 있습니다. 화면은 "승인 대기"·"승인된 기기" 탭으로 나뉘고, 새로고침 버튼으로 즉시 갱신할 수 있습니다(4초 자동 갱신도 계속 됩니다). 한 번 승인된 IP+기기 조합은 이후 비밀번호만으로 바로 접속됩니다.
- **서버가 도는 이 PC에서 직접 연 브라우저는 자동 승인**됩니다(대시보드를 그 PC에서 열 때) - 이미 그 PC 앞에 있다는 사실 자체가 승인과 같은 수준의 신뢰이기 때문입니다. 원격(다른 기기·Tailscale 등 프록시를 거친 접속)만 승인 절차를 거칩니다.
- **관리자 페이지 이중 보안**: 1) 이 서버가 도는 PC의 loopback 에서 온 요청만 인정하고, 프록시를 거친 흔적(X-Forwarded-For)이 있으면 겉보기엔 로컬이어도 원격으로 보고 거절합니다. 2) 그것만으론 같은 PC의 다른 프로그램까지 허용하는 셈이라, 트레이가 실행마다 새로 만드는 비밀 토큰까지 요구합니다 - 트레이 메뉴로 열 때만 URL 에 실립니다.
- **거부된 기기는 1시간 동안 재시도 차단**: 비밀번호를 맞힌 원격 상대가 거부당하자마자 곧바로 다시 시도해 관리자 페이지에 새 요청을 계속 띄우는 것을 막습니다(관리자가 나중에 승인하면 풀립니다).
- IP·User-Agent 는 접속을 시도하는 쪽이 마음대로 채울 수 있는 값이라, 관리자 페이지 렌더링에서 저장형 XSS(스크립트 주입)가 나지 않도록 반드시 안전하게(textContent) 표시합니다.
- **상단 밴드를 아이콘으로 정리했습니다.** 좌측(매매 상태)은 시장이 늘수록 계속 길어지던 글자 문구 대신 시장별 이모지(🇰🇷·🌍·🪙) + 우하단 점(모드 색)으로, 우측(프로그램 연결 상태)도 "프로그램 연결됨" 같은 글자 대신 아이콘(✅/🔄/⚠️/⏳)으로 바꿨습니다. 자세한 문구는 마우스를 올리거나(데스크톱) 눌러서(모바일, 기존 연결 표시등과 같은 방식) 볼 수 있습니다.

### 고친 것
- **★ 이 기능이 생기기 전에 로그인해 둔 세션 쿠키를 그대로 들고 있는 기기(휴대폰 등)는 새 기기 승인을 거치지 않고 계속 드나들 수 있었습니다**(승인 절차는 `/api/login` 때만 확인해서, 이미 유효한 쿠키가 있으면 그 확인 자체를 안 탔음 - 관리자 페이지 대기 목록에도 안 뜸). 이제 세션 쿠키가 있어도 요청마다 그 IP·기기가 지금 신뢰 목록에 있는지(또는 서버가 도는 PC에서 온 요청인지) 다시 확인합니다. **이 업데이트 이후 서버 PC가 아닌 곳(휴대폰 등)의 기존 로그인은 모두 풀리며, 다시 로그인하면 새 기기 승인 절차를 거쳐야 합니다** - 트레이 메뉴의 "새 기기 승인 관리"에서 한 번 승인해 주세요.
- **★ Groq 뉴스 필터가 등록 직후부터 계속 실패했습니다**: 기본 모델(`llama-3.3-70b-versatile`)이 Groq 에서 없어졌고(연결 테스트 결과 "서버 오류(404) - 모델이 존재하지 않음"), 지금 쓸 수 있는 모델(`openai/gpt-oss-20b/120b`)은 답을 내놓기 전에 속으로 생각하는 과정(reasoning)에 먼저 토큰을 써서, 그 과정을 줄여주지 않으면 짧은 응답 한도 안에서 생각만 하다 끝나 빈 응답으로 실패했습니다. 기본 모델을 `openai/gpt-oss-20b`로 바꾸고 요청에 `reasoning_effort: low`를 추가했습니다. 실제 등록된 키로 확인했습니다.
- **승인된 기기 목록에 무결성 서명을 추가했습니다.** devices.json 은 평범한 텍스트 파일이라 이 프로그램을 거치지 않고 직접 편집해 신뢰 목록에 기기를 끼워 넣을 수 있었습니다(승인 절차 전체를 무력화). 이제 저장할 때마다 이 PC에만 있는 비밀 키로 서명하고, 불러올 때 서명이 안 맞으면(=밖에서 손댄 흔적) 그 파일을 통째로 버리고(`.tampered-*` 로 백업) 빈 상태로 다시 시작합니다 - 모든 기기가 다시 승인받아야 하지만, 조작된 목록을 안전한 것으로 착각하는 것보다 낫습니다. (참고: 승인 목록은 원래도 재시작하면 그대로 유지됩니다 - 오늘 여러 번 사라진 건 개발 중 재빌드마다 테스트 파일을 정리하며 실수로 함께 지웠기 때문입니다.)
- **★ 같은 휴대폰인데도 접속할 때마다 다시 승인해야 했습니다**: 기기를 IP+User-Agent 로 식별했는데, 휴대폰은 와이파이↔데이터 전환 등으로 IP 가 자주 바뀌어 매번 "새 기기"로 보였습니다. 이제 브라우저에 심어 두는 오래가는 쿠키(약 400일)의 무작위 토큰으로 식별합니다 - IP 가 바뀌어도 그 쿠키만 있으면 같은 기기로 인식되어 한 번 승인하면 계속 유지됩니다(쿠키를 지우면 - 사생활 보호 모드 등 - 새 기기로 취급되어 다시 승인이 필요합니다). **이 변경으로 기존에 승인해 둔 기기도 이번 한 번은 다시 승인해야 합니다.**
- **★ fast 스타일의 손절폭을 2.5%→1.8%로 좁혔던 것을 되돌렸습니다.** 실거래(모의매매)에서 승률이 눈에 띄게 나빠지는 게 바로 확인됐습니다(해외 3건이 1초 간격으로 거의 동시에 손절되는 등) - scalp 에서 이미 본 "폭을 좁히면 정상적인 변동에도 쉽게 털린다"는 패턴이 fast 손절에서도 재현된 것으로 보입니다. fast 는 이제 **손절폭은 보통과 동일(2.5%, 암호화폐도 2.5%)**하고, 익절폭만 보통보다 좁습니다(4%→3.2%, 암호화폐 5%→4%).

---

## v0.10.0

### 바꾼 것
- **AI 는 뉴스·공시 위험 필터에만 씁니다.** Groq API 키를 2개까지 등록합니다(준비·연결 ④). 키 1의 무료 한도가 차거나 오류가 나면 키 2로 자동 전환하고(한도 초과 30분·키 오류 6시간 쉼), 둘 다 안 되거나 키가 없으면 지금처럼 키워드(규칙) 기반으로 판단합니다. 어떤 실패도 매매를 막지 않습니다(통과).
- Anthropic 키 등록은 없앴습니다. 일일 복기 메시지의 AI 코멘트도 없앴고, 복기는 계속 규칙 기반으로만 작성합니다.
- 설정: `news.groq_model`(기본 llama-3.3-70b-versatile). 키별 지우기·연결 테스트(키마다 결과 표시) 추가.
- 발급: console.groq.com 에서 가입만 하면 무료로 키를 받을 수 있습니다. 보내는 것은 종목 이름과 공개 헤드라인뿐입니다(계좌·금액·다른 키는 보내지 않음).
- 국내주식 진행 중 뜬 버그 세 가지를 고쳤습니다: 대시보드에 국내 현금 잔고 표시, 개장 초반 레인지 계산이 아직 안 찼을 때(NaN) 종목선정·스냅샷 응답이 통째로 실패하던 문제, **재시작할 때마다 모의투자 현금이 오늘 손익과 무관하게 배정액 그대로 초기화되던 문제**(보유 중인 포지션은 복원하면서 그 값어치만큼의 현금은 안 돌려줘 이중 계산됐음 - 이제 오늘 실현손익을 더하고 보유 포지션에 묶인 돈을 뺀 값으로 복원합니다).
- 국내·해외 익절폭을 5% → 4%로 낮췄습니다(`risk.take_profit_pct`).
- **빠른 단타(fast)와 보통(normal)의 손절·익절 폭을 분리했습니다.** 예전엔 fast 가 속도·기법만 다르고 폭은 보통과 같았는데, 이제 fast 는 국내·해외 익절 4%→3.2%·손절 2.5%→1.8%, 암호화폐 5%→4%·2.5%→1.8%로 보통보다 좁고 scalp(익절 2.4%·손절 1.2%)보다는 넓습니다. 이 폭은 아직 시뮬레이션으로 검증되지 않았습니다 - 모의매매로 확인 후 쓰세요.

---

## v0.9.0

### 추가한 것
- **AI 뉴스·공시 위험 필터**(국내·해외·암호화폐 공통): Anthropic API 키를 등록하면(준비·연결 ④), 사기 직전에 그 종목의 최근 헤드라인(구글 뉴스, 제목·출처·시각만)을 AI 가 읽고 상장폐지·횡령·유상증자·소송·리콜·해킹 같은 악재가 있으면 사지 않습니다.
  - **거르기 전용**: AI 가 사라고 하지는 않습니다. 틀려도 손실이 아니라 놓친 기회로 끝납니다.
  - **실패하면 통과**: 키가 없거나 네트워크·AI 오류·하루 호출 상한 초과면 필터가 조용히 빠지고 기존 매매 로직만 돕니다. (news.mode=avoid 이면 위험 키워드로 제외 - 이 모드는 예전에는 엔진에 연결돼 있지 않아 작동하지 않았습니다.)
  - **개인정보**: AI 로는 종목 이름과 공개 헤드라인만 보냅니다(계좌·금액·다른 키 없음). 헤드라인은 '믿을 수 없는 입력'으로 다루고, 응답은 정해진 JSON 형식만 받아들입니다. 종목별 60분 캐시, 하루 300회 상한(설정).
  - 후보는 스크리닝 때 백그라운드로 미리 판정하고, 살 때 결과가 없으면 그 자리에서 한 번 묻습니다.
- **하루 매매 복기 텔레그램 발송**: 국내는 장 마감 10분 뒤(15:40), 미국은 **한국시간 아침 06:00**(직전 미국 거래일), 암호화폐는 **매일 21:00**(직전 24시간)에 성과(건수·승률·손익·손익비)·기법별·청산 사유별 성적·추가 매수/분할 매도 사용·점검 포인트를 보냅니다. Anthropic 키가 있으면 AI 코멘트 3문장을 덧붙입니다(집계 숫자만 전송). 그날 거래·보유가 없으면 보내지 않고, 보낸 기록을 파일에 남겨 서버를 다시 켜도 중복 발송하지 않습니다. 준비·연결 화면에서 지금 받아 볼 수도 있습니다.
- 설정: `news.ai_filter/ai_model/ai_max_calls_per_day`, `notify.daily_review/review_ai/overseas_review_time/crypto_review_time`.

---
## v0.8.0

### 추가한 것
- **국내 장 초반·막판 변동성 단타**: 09:00~스캔 시작(장 초반)과 스캔 종료~15:00(장 막판)에 그 시간대 전용 기법만 새로 살 수 있습니다(`entry.extra_windows`, 시각은 설정 가능). 나머지 기법은 기존 매매 시간(09:20~14:00)에만 씁니다.
  - **시초 갭 돌파(open_gap)**: 전일 종가 대비 +1~8% 갭으로 시작해 갭을 지키며(시가 위 유지) 개장 첫 5분 고점을 거래량 1.5배와 함께 뚫고 VWAP 위일 때. 09:40 이후 작동 안 함.
  - **장 막판 상승 지속(close_squeeze)**: 14:30~15:00, 당일 +3% 이상 강한 종목이 고점 0.7% 이내를 지키고 VWAP 이 오르며 막판 거래량이 1.3배 이상일 때. 15:10 강제 청산까지 짧게 보유.
  - 진입 기법에서 켜야 작동하며(빠른 단타 모드는 국내에 자동으로 켬) 해외·암호화폐에는 넣지 않습니다. **프리마켓(NXT)은 지원하지 않습니다** - 시세·주문 지원을 확인하지 못했습니다.
- **테마 근거의 크기·장중 변동성에 따른 금액 자동 조절**(`sizing.dynamic`, 국내·해외·암호화폐 공통):
  - 첫 매수 배수 = 신호 강도 60% + 테마 근거 크기 40%(테마 순위·동반 상승 종목 수·상승률 중앙값·대장주 여부; 관심 종목은 중립 0.5, 암호화폐는 전날 거래대금 순위) × 변동성 배수(분봉 ATR 이 크면 적게 0.6~1.2배).
  - 추가 매수는 근거가 큰 종목을 더(0.7~1.3배), 분할 매도 비율은 근거가 크면 덜·출렁임이 크면 더(15~60%).
  - 매수 기록에 신호 강도·테마 근거·변동성 배수가 남습니다.

### 참고(시뮬레이션, 실제 시장 아님)
- 자동 조절 켬/끔: 총 손익 +31%(8시드)·+9%(다른 8시드), 손익비는 2.33 vs 2.41 / 2.76 vs 3.59 - 사실상 중립. 효과를 확신할 근거는 없습니다.
- 빠른 단타(+새 기법·시간대): 49건·승률 61%·손익비 2.27, 보통: 45건·60%·2.11.

---
## v0.7.1

### 바꾼 것
- 암호화폐 손절 2.5%·익절 5%로 주식과 동일하게 맞춤(기존 5%/10% - 실제 기록에서 10% 이상 오른 거래는 102건 중 1건뿐이었음).

### 점검
- 국내 추가 매수(피라미딩) 경로 테스트 추가(장부 반영·평균 매수가·한도·횟수 상한·전량 청산 기록), 설정 화면 저장 왕복(새 항목·퍼센트 변환) 확인, 전체 테스트·정적 분석 통과.

---
## v0.7.0

### 큰 변경: 투자금액 배분·분할 매수·분할 매도 (국내·해외·암호화폐 공통)
- **시장별 투자금액**: 국내 `capital.allocation`(초기 1,000만원), 해외 `overseas.budget_usd`(초기 1만 달러), 암호화폐 `crypto.budget`(초기 1,000만원). 종목당 한도 = 투자금액 ÷ 동시 보유 수 - 어떤 경우에도 넘지 않습니다. 예전의 1회 투입 비율·배정 비율·코인당 금액은 없어졌습니다.
- **신호 강도에 따라 매수 금액 변동**: 진입 기준을 얼마나 여유 있게 넘었는지(필수 항목 여유의 평균)로 강도를 재고, 첫 매수 = 종목당 한도 × 50% × 배수(0.6~1.4).
- **분할 매수(피라미딩)**: 이익 중인 종목에만, 진입 신호가 살아 있을 때, 마지막 매수가보다 손절폭의 40% 이상 오르면 남은 몫을 나눠 추가(기본 2회). 손실 중에는 절대 추가하지 않습니다(물타기 금지). 평균 매수가를 다시 계산하고 서버 OCO도 새 수량으로 다시 겁니다.
- **분할 매도**: 익절폭의 절반에서 보유량의 34%, 익절폭에서 남은 수량의 50%, 나머지는 추적 손절이 끌고 갑니다. 1차 뒤 본전 아래로 내려오면 나머지를 정리(본전 방어). 손절·시간·강제 청산은 분할 없이 전량. 거래 기록은 마지막에 한 건으로 합쳐(총 수량·평균 청산가·전체 손익, 추가/분할 횟수 표기) 건수·승률이 부풀지 않습니다.
- 보유 종목 표에 추가 매수(＋n)·분할 매도(－n) 상태 표시.

### 설정 화면: 필수 / 선택 / 자동으로 분류
- **필수**(12개): 시장 사용 여부·거래 모드·시장별 투자금액과 동시 보유 수·매매 속도. **선택**: 직접 추가 관심 종목. **자동**: 나머지 전부 - 접힌 "자동 설정"에 모아 두고 필요할 때만 수정. 옛 config 에 없는 새 항목은 기본값으로 채워 보여줍니다.

### 매매 속도(style)
- `normal`(기본) / `fast`(시세 확인 주기·재선정·재진입 금지를 짧게, 일일 거래 한도 확대, 진입 기법 6개 추가; 손절·익절 폭은 그대로) / `scalp`(fast + 작은 목표 익절 2.4%·손절 1.2%, 실험용).
- ⚠ 시뮬레이션(시나리오 4종 × 시드 8개): 보통 승률 70%·손익비 2.8, fast 손익비 약 1.8~2.4(거래 수만 소폭 증가), scalp 승률 13~20%·손익비 0.13 이하로 크게 손실. 목표를 낮추면 이익이 늘어난다는 근거는 없습니다. 시뮬레이션은 실제 시장이 아니므로 모의매매로 확인하세요.

### 웹 접근: 자리 비움 잠금
- 화면을 아무도 조작하지 않은 채 30분(설정 `ui.session_idle_minutes`, 0=끔)이 지나면 비밀번호를 다시 받습니다. 자동 갱신은 조작으로 치지 않으며, 서버도 같은 기준으로 세션을 끝냅니다.

### 고친 것
- 해외주식 일일 손실 한도 계산이 달러 손익을 원화 배정액과 비교하던 단위 오류를 바로잡았습니다.

---
## v0.6.3

### 고친 것
- 모바일에서 시장 메뉴의 마지막 탭(환율·코인)과 뒤쪽 탭·필터 버튼이 가로 스크롤에 가려 안 보이던 문제: 한 줄에 안 들어가면 줄바꿈해 모두 보이게 했습니다.

---
## v0.6.2

### 바꾼 것
- 대시보드 보유 종목 표의 진입가 옆에 **현재가** 열을 추가했습니다.
- 성과(거래 내역)에서 날짜 열을 빼고 매도일시 최신순으로 정렬, 매매일지는 거래 한 건을 한 줄로 묶어 매수일시·매도일시를 넣고 매도일시 최신순으로 정렬했습니다(보유 중인 건 맨 위).

### 고친 것
- **평가손익이 고점 기준이던 문제**: 엔진이 마지막 현재가를 기록하지 않아, 화면의 평가손익·손익 추이의 미실현 부분이 현재가가 아니라 보유 중 최고가로 계산되고 있었습니다. 이제 국내·해외·암호화폐 모두 마지막으로 받은 현재가를 기록하고 화면이 그 값을 씁니다(시세를 못 받았으면 예전처럼 고점으로 어림).

---
## v0.6.1

### 고친 것
- **종목선정 화면 깜빡임**: 15초 폴링·스트림마다 패널을 통째로 지우고 다시 그리던 것을, 내용이 달라졌을 때만 바꿔 끼우도록 했습니다(반복 갱신 시 화면 변경 0건 확인).
- **표 제목 아래 네모 아이콘**: 열별 필터 입력칸이 모든 표에 붙어 있던 것을 기본으로 껐습니다(정렬은 그대로).
- **"연속 손실로 매매 규모를 줄인 상태" 문구와 실제 조건 불일치**: 문구는 '연속 손실'인데 실제로는 오늘 실현손익이 조금이라도 마이너스면 1회 매매 규모를 절반으로 줄이고 있었습니다. 이제 연속 손절이 기준 횟수(`risk.max_consecutive_losses`, 기본 2회) 이상일 때만 줄이고, 이익이 나면 원래대로 돌아옵니다. 규칙 자체(잃고 있을 때 크기를 키우지 않는다)는 유지합니다.

---
## v0.6.0

### 추가한 것
- **새 진입 기법 'VWAP 재탈환'(vwap_reclaim)**: VWAP 아래로 밀렸던 종목이 거래량을 동반한 양봉으로 VWAP 위로 다시 올라서는 순간을 잡습니다(실패한 하락의 되돌림). 기존 'VWAP 눌림목'이 위에서 쉬다 가는 종목이라면 이건 아래에서 되돌리는 종목입니다. **기본은 꺼짐**이라 설정 → 진입 기법에서 켜고, 실험실의 'VWAP 재탈환만' 프리셋으로 비교해 볼 수 있습니다.
- **해외주식 보유 종목 관리 주기 60초**(`overseas.manage_seconds`): 진입 탐색은 5분마다 그대로 하고, 이미 산 종목의 손절·익절만 1분마다 확인합니다. 5분에 한 번만 보면 -2.5% 손절선을 지나친 뒤에야 파는 일이 생길 수 있었습니다.

### 검토한 것(바꾸지 않음)
- 시뮬레이션(시나리오 4종 × 시드 8개)에서 기존 구성 40건·승률 57%·손익비 2.43, 재탈환을 더한 구성 41건·59%·2.72로 사실상 같고, 재탈환만 켰을 때는 신호가 0건이었습니다(가짜 시장에는 그런 패턴이 없음). 실제 시장 근거가 없어 기본 꺼짐으로 뒀습니다.
- 암호화폐 모의매매 기록(74건, 5일)은 표본이 작아 기법·설정 변경의 근거로 쓰지 않았습니다(RSI 되돌림 0승 9패, 변동성 돌파 35% 승률 등은 참고만).

---
## v0.5.1

### 바꾼 것
- **종목 선정 화면 개편**: 통합 탭은 선정된 국내·미국·암호화폐 종목을 한 표로 보여줍니다. 국내·해외 탭은 전체 테마 종목을 한 줄씩 나열해 선정(✓)·탈락과 그 기준·이유("테마 1위 · 동반상승 3종목 · 대장주" / "거래대금 1,000억 미만" / "테마 미인정 - 동반 상승 1종목 < 기준 2종목")를 간결하게 보여줍니다. 선정만/전체 전환, 관심 종목 표시, 보유 여부 포함. 암호화폐 탭은 전날 거래대금 순위를 보여줍니다. 예전의 카드 뭉치와 테마별 펼침 표는 없앴습니다.
- 대시보드 감시 카드의 미국 안내 문구를 테마주+관심 종목 기준으로 수정.

### 점검·보안
- 시세 차트 API의 종목 식별자를 시장별 형식으로 검증(경로 조작·쿼리 주입 차단), 국내 차트 봉 수 상한, 미국 테마 새로고침 최소 30초 간격, 빗썸 조회 실패 메시지 마스킹.
- 사용하지 않는 선정 화면 코드 140줄 정리, 정적 분석(pyflakes)에서 정의되지 않은 이름 없음 확인.

---
## v0.5.0

### 추가한 기능
- **미국 주식 24시간 거래**: 평일 24시간(일요일 저녁 8시~금요일 저녁 8시 뉴욕시각) 거래하고, 주말과 NYSE 휴장일(새해·MLK·대통령의 날·성금요일·메모리얼·준틴스·독립기념일·노동절·추수감사절·크리스마스)만 쉽니다. 설정 `overseas.trade_24h`로 정규장만으로 되돌릴 수 있습니다. 정규장 밖에서는 마지막 시세가 15분 넘게 낡았으면 신규 진입을 건너뜁니다.
- **미국 테마주 자동 산정**: 미국 종목을 14개 테마(AI반도체·반도체장비·AI소프트웨어·빅테크·전기차·원전·방산우주·바이오·핀테크·사이버보안·로봇·클린에너지·석유·금융)로 묶고, 국내와 같은 점수(동반상승 종목수 × 상승률 중앙값 × log10 거래대금)로 오늘의 테마와 종목을 자동으로 뽑아 거래 대상에 넣습니다. 실행 파일 옆에 `us_themes.yaml`을 두면 그 사전으로 바꿔 씁니다.
- **장이 바뀔 때마다 재선정**: 프리마켓 → 정규장 → 애프터마켓 → 야간이 바뀔 때, 휴장 → 개장 때, 그리고 설정한 주기(기본 60분)마다 다시 뽑습니다.
- **관심 종목은 테마와 별개로 항상 거래 대상**: 국내(`screen.watchlist`, 6자리 코드)와 미국(`overseas.watchlist`) 모두, 테마주 외에 직접 추가한 관심 종목이 함께 거래 대상 후보가 됩니다. 국내 관심 종목은 거래정지·경고 종목만 안전상 제외합니다.
- 해외 종목선정 화면에 오늘의 미국 테마주(후보 표·테마별 판정)와, 각 종목이 테마주인지 관심 종목인지 표시.
- 준비·연결 화면의 토스·빗썸·텔레그램 입력칸에 항목 이름(Client ID / Client Secret / Access Key / Secret Key / 봇 토큰 / 채팅 ID)과 등록 상태를 표시. 저장된 비밀값은 다시 보여주지 않고 식별자만 끝 4자리를 보여줍니다.

### 고친 것
- 저장 위치 안내 문구를 현재 방식(암호화된 secrets.yaml)에 맞게 바로잡았습니다.

---
## v0.4.1

### 추가한 기능
- **종목 시세 · 매매 시점 그래프**: 국내·해외·암호화폐 대시보드에 종목을 골라(● 보유 · ✓ 오늘 매매 · 감시 목록)
  분봉 시세 위에 매수(▲)·매도(▼) 시점을 표시하는 카드를 추가했습니다. 우측 가격 눈금과 하단 시각 눈금이 함께 나옵니다.
- **상세보기 팝업**: 카드의 "상세보기"를 누르면 그래프만 크게 보여줍니다(15초마다 갱신, 휠·핀치 확대, 드래그 이동, 더블클릭 전체).
- 매수·매도 표시를 크게: 봉 뒤 색 띠, 굵은 세로선, 체결가 원, "▲ 매수 가격" / "▼ 매도 가격" 색 라벨(겹치면 위아래로 밀어냄).
- 진행 로그·표처럼 글자가 잘리는 영역은 마우스로 좌우로 끌어서(모바일은 스와이프) 끝까지 볼 수 있습니다.
- 종목 시세 차트 확대·축소: 본문에서 휠 = 시간·가격 동시, 오른쪽 가격 눈금 위 = 가격축만, 아래 시각 눈금 위 = 시간축만. ↔＋/－, ↕＋/－, ⟲ 버튼과 모바일 핀치(가로=시간, 세로=가격)·끌어서 이동·더블클릭/더블탭 초기화를 지원합니다.
- 계좌 조회 표(토스 국내·해외, 빗썸)에서 열 아래 필터 입력칸(작은 네모)을 뺐습니다.
- 빗썸 계좌 조회가 거절되면 빗썸이 준 사유(키 오류·IP 미등록·권한 없음)를 화면에 그대로 보여줍니다.
- 해외·암호화폐용 시세 API `/api/market-chart/{market}/{symbol}` 추가.

### 고친 것
- 화면이 1~5초마다 통째로 다시 그려져 깜빡이던 문제: 내용이 그대로면 그리지 않습니다. 손익 차트는 한 번만 만들어 재사용하므로 줌 상태도 유지됩니다.
- 손익 차트 두 손가락 확대·축소, 더블탭 초기화(모바일).
- 모바일 화면에서 지표·탭·버튼·표·로그를 한 줄로 배치하고, 보유 종목 표의 기법 열은 뺐습니다.

---

## v0.4.0

### 추가한 기능

- **암호화폐: 전날 거래대금 상위 10종목만 감시** — 매일(KST 날짜가 바뀐 뒤 처음) 빗썸 KRW 마켓 중 전날 일봉
  거래대금 상위 10종목(스테이블코인 제외)을 뽑아 그것만 감시합니다. 선정에 실패하면 기존 목록으로 되돌아가고,
  이미 보유한 코인은 목록에서 빠져도 청산 관리를 계속합니다. 설정: `crypto.auto_top_volume`,
  `crypto.top_volume_count`(기본 10), 동시 보유 상한 `crypto.max_positions`(기본 5). 설정 화면·상태 API·
  종목 선정/대시보드 안내 문구를 모두 맞췄습니다.
- **암호화폐: 손절 후 같은 코인 재진입 대기(기본 30분)** — 실제 이력 분석에서 손절 직후 30분 안에 같은 코인을
  다시 산 10건이 전부 손실(승률 0%)이었습니다. 국내·해외에는 있던 손실 후 쿨다운이 암호화폐에만 없었습니다.

### 설정 변경 (매매원칙 점검 결과)

- **1회 투입 45% → 33%** — 45% × 동시 보유 3종목 = 135%로 배정 자본을 넘는 구조라 "먼저 잃지 않는다"·"신용·미수
  금지" 원칙에 어긋났습니다(33% × 3 = 99%).
- **최대 등락률 20% → 12%** — 이미 20%나 오른 종목까지 사는 것은 매매원칙의 "추격매수 금지"와 어긋납니다.
- 실제 모의매매 이력: 국내 6건(전부 손실, 표본이 너무 작아 결론 불가), 암호화폐 67건(승률 24%, 손익비 1.54, 총
  +4,973원 - 손실 22건이 전부 ATR 손절, 이익은 momentum_fade 청산에서 나옴), 해외 9건(거의 0). 승률이 낮은 것은
  추세 추종형의 특성(작게 잃고 가끔 크게 버는 구조)에 가깝고, 손익은 이익 구간을 남겨 두는 청산에서 나옵니다.
- 시뮬레이션 비교(시나리오 4종 × 시드 16개, 설정 변형 12가지): 손절 2%/익절 4%, ATR 손절 배수 변경·끄기, 진입
  확인 강화, 트레일링 변경은 시드에 따라 결과가 뒤집히거나 기준보다 나빠서 **채택하지 않았습니다**. 채택한 두 변경은
  처음 보는 시드에서도 승률 62~70%, 손익비 2.0~2.6으로 기준(60%, 1.95)보다 나쁘지 않았습니다(절대 손익은 투입
  축소만큼 약 25% 줄고 자본 대비 효율은 비슷). ⚠ 시뮬레이션은 실제 시장을 재현하지 않으며 표본이 작아 **수익을
  보장하지 않습니다** - 실제 모의매매 이력이 더 쌓이면 다시 점검하세요.

### 고친 버그

- **통합 탭의 국내주식 보유가 `null종`으로 나오던 것**(꺼져 있을 때 값을 모르는데 그대로 문자열에 넣음) → `-`.

---
## v0.3.4

### 고친 버그

- **성과 수치가 화면마다 조금씩 안 맞던 것** — ① MDD: 국내는 "일별 누적", 해외·암호화폐·통합은 "거래 단위"로 계산해서
  같은 화면의 값끼리 어긋났습니다. 모두 거래 단위(청산 시각순)로 통일했습니다. ② 해외·암호화폐의 일자별
  집계가 날짜를 UTC 로 잘라 한국시간 00~09시 거래가 전날로 들어가던 것을 로컬 날짜로 바로잡았습니다.
  ③ 승률: 대시보드는 무승부를 빼고, 성과는 넣어서 달랐던 것을 성과와 같은 정의로 맞췄습니다.
  ④ 통합 합산이 국내 거래를 최근 500건만 써서 500건이 넘으면 국내 행과 어긋나던 것을 5000건으로 늘렸습니다.
- **매수·매도·손익 금액이 안 맞아 보이던 것** — 매수 금액에는 보유 중인 종목의 원가까지 들어가고 매도는
  청산분만, 손익은 수수료·세금을 뺀 값이라 "매도 − 매수 = 손익"이 성립하지 않았습니다. 계산은 그대로 두고
  매도 카드에 청산분의 매수 원가, 손익 카드에 반영된 비용을 함께 표시합니다.

---
## v0.3.3

### 변경

- **[성과] 통합은 요약만** — 거래내역·일자별·월별·기법별 세부 표 없이, 세 시장을 합친 요약(누적 손익·거래·승률·손익비·MDD)과 시장별 요약 표만 보여줍니다. 세부 표는 각 시장 탭에서 봅니다.

---

## v0.3.2

### 변경

- **[준비·연결] 바깥 연동 → 외부 연동으로 이름 변경**(탭·안내 문구 전체).
- **[종목 선정] 통합은 국내주식 선정 종목만** — 해외주식·암호화폐는 모두 감시 대상이라 통합에서 뺐습니다.
- **국내 대시보드 진행 로그를 해외·암호화폐와 같은 "최근 청산" 요약으로** — 엔진 내부의 길고 어수선한
  실시간 로그 대신, 청산 이력(원장)을 `[시간] 종목 청산 · 사유 · 손익` 한 줄씩, 날짜 머리글로 묶어
  보여줍니다. 자동매매가 꺼져 있어도 과거 이력이 나옵니다.

---
## v0.3.1

### 고친 버그

- **국내 진행 로그가 비어 있던 것** — 실시간 스트림으로 "지금부터" 오는 줄만 받아서, 국내 자동매매가
  꺼져 있으면 아무것도 안 나왔습니다. 화면을 열 때 이미 쌓인 로그를 불러오고, 그것도 없으면 가장
  최근 매매일지(매수·매도)를 진행 로그로 보여줍니다.
- **통합 탭에서 감시 카드와 진행 로그 제외** — 통합은 지표·손익 추이·보유만 보여줍니다.
- **[종목 선정] 통합은 선정된 종목만** — 세 시장의 선정 종목(국내 후보, 해외·암호화폐 감시 목록)을
  한 표로만 보여주고, 선정 과정은 각 시장 탭에서 봅니다. 행을 누르면 그 시장 탭으로 이동합니다.
- **[매매일지] 통합은 요약만** — 그날 시장별 매수·매도 건수, 익절·손절, 실현손익만 보여줍니다.
  상세 이력은 각 시장 탭에서 봅니다.
- **[시장] 그룹 제목 제거** — 그룹이 탭으로 나뉘어 있어 탭 안의 그룹 제목은 뺐습니다.

---

## v0.3.0

### 보안 강화 (전체 점검)

- **민감한 작업은 서버가 비밀번호 재확인을 강제** — 자동매매 시작·정지·전량 청산, API 키·설정·테마
  저장, 비밀번호 변경, 성과 초기화 등은 이제 로그인 쿠키만으로는 서버가 받지 않고, "방금 비밀번호를
  다시 입력했다"는 짧은 수명 토큰(자동매매 90초, 설정 10분)이 있어야 합니다. 예전엔 화면에서만
  재확인해서 세션 쿠키만 있으면 API 를 직접 불러 우회할 수 있었습니다. 토큰이 만료되면 화면이
  알아서 비밀번호를 다시 묻고 같은 동작을 이어서 실행합니다.
- **세션 쿠키 보강** — 발급시각·서명을 넣어 30일 뒤 자동 만료(예전 365일), `SameSite=Strict`,
  HTTPS(Tailscale)로 들어오면 `Secure`. 비밀번호를 바꾸면 이전 쿠키가 모두 무효가 되는 것은 그대로.
- **DNS 리바인딩·CSRF 방어** — 허용된 주소(localhost·IP·`*.ts.net`)가 아닌 Host 는 거절하고, 쓰기
  요청의 Origin 이 다른 사이트면 거절합니다. 다른 이름을 쓰려면 환경변수 `DAYTRADER_ALLOWED_HOSTS`.
- **보안 헤더·CSP** — 인라인 스크립트를 막는 Content-Security-Policy, `X-Frame-Options: DENY`,
  `nosniff`, `Referrer-Policy: no-referrer`, API 응답 `no-store`, HTTPS 면 HSTS. 화면에 어떤 문자열이
  섞여 들어와도 스크립트가 실행되지 않습니다. `/docs`·`/openapi.json` 은 로그인 없이 API 목록을
  보여 주므로 껐습니다.
- **오류·로그에서 API 키 가리기** — 요청 실패 메시지는 URL 째로 들어 있는 경우가 많고 텔레그램은
  URL 자체에 봇 토큰이 들어갑니다. 화면에 나가는 오류와 로그 파일·진행 로그 모두에서 토큰·키·Bearer
  값을 `***` 로 바꿉니다.
- **비밀번호** — 상수 시간 비교, 뻔한 6자리(기본값·같은 숫자·연속 숫자) 설정 금지, 기본 비밀번호를
  쓰는 동안 화면 상단 경고 배너. 프록시(Tailscale) 뒤에서도 접속자별로 잠금(3회 실패부터 1분씩)을
  셉니다(같은 PC 에서 온 헤더만 믿어 주소 위조로 우회할 수 없음).
- **뉴스 링크** — `javascript:` 같은 주소는 링크로 쓰지 않습니다. 종목·테마 이름은 HTML 이스케이프.

### 고친 버그

- **트레이의 "매매 중단" 메뉴와 상태 아이콘이 401 로 막혀 있던 것** — 로그인 기능을 넣은 뒤로
  트레이가 서버 API 를 로그인 없이 불러 아이콘 색·툴팁 갱신과 비상 정지가 동작하지 않았습니다.
  트레이가 서버를 띄울 때마다 새로 만든 비밀 토큰을 같은 PC 에서만 인정하는 통로로 고쳤습니다.
- **좌측 상단 밴드에서 국내주식 모드가 안 보이던 것 / 표현 통일** — 꺼져 있어도 "국내주식: 꺼짐
  (모의매매)"로 항상 표시하고, 순서를 국내 → 해외 → 암호화폐로, 실행 중 표현을 모두 "○○ 중"으로
  맞췄습니다(국내만 "모드"라고 쓰던 것).
- **준비·연결 탭이 오래 걸리던 것** — 네트워크 진단이 세 번씩 돌고, 서버가 외부 접속을 기다리는 동안
  다른 요청을 막고, 화면이 느린 것까지 전부 기다렸다 그렸습니다. 진단 결과를 30초 캐시하고 해당
  핸들러를 스레드풀에서 돌리며, 화면은 빠른 것부터 먼저 그리고 진단·사전 점검은 뒤에서 채웁니다.
- **메뉴를 열 때마다 매번 다시 불러오던 것** — 이미 불러온 화면은 시간 안에 다시 열면 그대로
  보여줍니다(성과·매매일지 1분, 매매 기법·매매원칙·설정 등 5분).
- **매매일지 기본 날짜가 매매 없는 날로 잡히던 것**, 서버가 날짜 없이 부르면 전 기간을 한꺼번에
  내려보내던 것.

### 추가한 기능

- **대시보드 4개 탭 통일(통합·국내·해외·암호화폐)** — 같은 순서·같은 항목: 자동매매 상태(한 줄) →
  지표(오늘 매매 / 오늘 성과 / 현재 보유) → 손익 추이(오늘) → 보유·감시 표 → 진행 로그. 시장마다
  달랐던 상태 카드·보유·감시 카드를 한 벌의 부품으로 합쳤고, 통합 탭에는 시장별 상태 표를 넣었습니다.
  해외·암호화폐 지표가 "최근 10건" 기준이던 것을 국내와 같은 "오늘" 기준으로 맞췄습니다.
- **Records 통일** — 성과: 네 시장 모두 같은 요약(누적 손익·거래·승률·손익비·MDD)과 하위 탭. 매매일지:
  네 시장 모두 같은 컬럼(시간·시장·모드·구분·종목·손익·내용)과 날짜 선택, 해외·암호화폐도 매수·매도
  이력으로 표시. 기본 필터는 모두 "통합".
- **탭으로 나눈 메뉴** — 시장(국내 / 해외·선물 / 미국 종목 / 환율·코인), 매매원칙(섹션별), 매매 기법
  (진입 / 청산 / 켜진 기법 / 오늘·실적), 준비·연결(한눈에 / 외부 연동 / 계좌 현황 / 진단·점검), 속보
  (그룹별). 선택한 탭은 자동 갱신돼도 유지됩니다.
- **진행 로그를 날짜별로 그룹핑** — 날짜는 `2026-09-21 (월)` 머리글로 한 번만, 각 줄에는 시간만.

---

## v0.2.7

### 고친 버그

- **좌측 상단 밴드에서 국내주식 매매 모드가 안 보이던 것** — 국내 항목이
  엔진이 켜져 있을 때만 오는 실시간 스냅샷에 의존해서, 국내 자동매매가
  꺼져 있으면 항목 자체가 사라졌습니다. 이제 꺼져 있어도 "국내주식: 꺼짐
  (모의매매)"처럼 설정된 모드와 함께 항상 표시하고, 어느 탭에서든 최신으로
  갱신합니다. 표시 순서도 **국내주식 → 해외주식 → 암호화폐**로 맞췄습니다.

---

## v0.2.6

### 고친 버그

- **대시보드에 간헐적으로 뜨던 type 오류** — 원인은 `config.yaml`에 값이
  빈 채로 남은 항목(`overseas.max_positions:`)이었습니다. YAML은 빈 값을
  `None`으로 읽고, 그 `None`이 기본값(4)을 덮어써서 미국 장이 열려 있는
  동안 해외주식 엔진 루프가 매번 "'>=' not supported between instances
  of 'int' and 'NoneType'"으로 죽었습니다(장이 닫혀 있을 땐 그 줄에
  도달하지 않아 "간혹"처럼 보였습니다). 이제 기본값이 있는 설정 항목은
  빈 값이면 기본값을 씁니다(같은 종류의 다른 항목도 함께 보호됩니다).
- **해외·암호화폐·통합 그래프의 시간축 라벨이 엉뚱한 숫자로 나오던 것**
  (예: `17587`) — 시각이 밀리초 숫자로 오는 차트의 라벨 처리가 없었습니다.

### 추가한 기능

- **대시보드 그래프에 종목별 손익선 추가** — 국내주식·해외주식·암호화폐·
  통합 모두, 총 손익 선 옆에 종목별 손익(실현+평가)을 함께 그립니다.
  손익 절대값이 큰 상위 6종목까지 그리고, 범례를 누르면 선을 끄고 켤 수
  있습니다. 국내주식은 서버가 종목별 곡선을 분 단위로 쌓습니다.
- **대시보드 지표 그룹화** — 한 줄로 늘어서 있던 8개 지표를 **매매**(매수·
  매도) / **성과**(손익·익절·손절·승률) / **보유**(보유·평가손익) 세 묶음으로
  나눠 제목을 붙였습니다(국내·해외·암호화폐·통합 공통). 좁은 화면에서는
  묶음이 위아래로 쌓입니다.
- **손익 색 통일: 이익 = 빨강, 손실 = 파랑** — 표의 누적·MDD·수익금액·수익률
  열과 암호화폐·해외 매매일지의 손익, 진행 로그의 청산 줄(예전엔 손실이
  노랑 경고색이었음)에도 같은 색을 적용했고, 0은 색을 붙이지 않습니다.
  그래프 배경도 0선 위(이익)는 붉은 기, 아래(손실)는 푸른 기로 바꿨습니다.
- **그래프 축 라벨 정리** — y축 제목(손익(원)/손익($))과 x축 제목(시각/청산
  시각) 추가, 눈금은 부호+만/억 단위로 줄여 표시, x축 시각 라벨은 눈금에
  가운데 맞춰 양 끝이 잘리지 않게, 마우스를 올리면 전체 일시·정확한 금액이
  나옵니다.

---

## v0.2.5

### 고친 버그

- **장이 닫혀 있는데도 종목 선정을 하던 것** — 해외주식은 엔진 루프 맨
  앞에서 자동 선정(랭킹 조회)을 장 열림 여부와 상관없이 실행해서, 장이
  닫힌 동안에도 매 루프 랭킹 API를 호출했습니다(선정 실패 시엔 매분
  재시도). 이제 장이 열려 있을 때만 선정합니다. 국내주식도 엔진은
  원래 진입 시간대에만 선정했지만, [종목 선정] 화면을 열거나 "다시
  스크리닝"을 누르면 주말·휴장일·장 마감 후에도 스크리닝이 돌던 것을
  막았습니다(이땐 "지금은 종목을 선정하지 않습니다" 안내가 나오고, 오늘
  이미 뽑아 둔 결과가 있으면 그대로 보여줍니다). 시뮬레이션·리플레이는
  실제 장과 무관하므로 그대로 동작합니다.
- **릴리즈 노트를 볼 화면이 없던 것** — CHANGELOG.md가 exe에 포함되지도
  않았고 읽는 화면도 없었습니다.

### 추가한 기능

- **릴리즈 노트 화면** — [Improve] → "📝 릴리즈 노트" 탭, 또는 상단의
  버전 표시(v1.2.5)를 누르면 열립니다. 버전별로 정리돼 보이고 현재
  버전에 표시가 붙습니다.

---

## v0.2.4

### 추가한 기능

- **API 키·비밀번호 암호화 저장** — `secrets.yaml`의 모든 값(토스·빗썸
  키, 텔레그램 토큰, 화면 로그인 비밀번호, 세션 비밀키)을 Windows
  DPAPI로 암호화해서 저장합니다(`enc:...` 형태). 파일이 유출돼도 이
  컴퓨터의 같은 윈도우 계정이 아니면 복호화할 수 없습니다. 예전 평문
  값은 새 버전을 처음 켤 때 자동으로 암호화됩니다.
  ⚠ 다른 PC·다른 윈도우 계정으로 `secrets.yaml`을 옮기면 키를 못 읽으니
  그때는 키를 다시 입력해야 합니다.
- **비밀번호 연속 실패 잠금** — 3번째 실패부터 잠기고 틀릴 때마다 1분씩
  늘어납니다(3회=1분, 4회=2분...). 잠긴 동안엔 맞는 비밀번호도 거부되며,
  성공하면 초기화됩니다. 로그인·설정 진입·자동매매 시작/정지 확인에 모두
  적용됩니다(접속 IP별, 서버 재시작 시 초기화).
- **성과 화면 개선** — 거래내역은 실제 매도 시각 기준 최신순, 매수·매도
  일시를 `yyyy-MM-dd HH:mm:ss`로 표시. 일자별·기법별에 "마지막 매도일시"
  컬럼 추가, 일자별·월별·기법별 모두 최신순 정렬.
- **진행 로그에 날짜 추가** — 국내·해외·암호화폐·통합 진행 로그 모두
  `[yyyy-MM-dd HH:mm:ss]`로 표시(최신이 맨 위).

### 고친 버그

- 해외·암호화폐·통합 진행 로그의 시각이 UTC로 표시되던 것을 로컬(KST)
  시각으로 바로잡았습니다.

---

## v0.2.3

### 추가한 기능

- **자동매매 시작·정지에도 비밀번호 재확인** — 국내주식·해외주식·암호화폐
  세 시장 전부, "자동매매 시작"·"정지"·"전량 청산 후 정지" 버튼을 누르면
  실제로 실행되기 전에 비밀번호를 한 번 더 확인합니다. v1.2.2의 [설정]
  메뉴 재확인과 같은 방식으로, 로그인 세션이 있어도 계좌를 움직이는
  조작만큼은 한 번 더 막습니다.

---

## v0.2.2

### 추가한 기능

- **[설정] 메뉴에 들어갈 때마다 비밀번호 재확인** — 로그인 세션이 이미
  있어도, 자리를 비운 사이 잠금 안 된 화면을 다른 사람이 주웠을 때 API
  키·자동매매 시작/정지 같은 민감한 설정까지 바로 볼 수 있는 건 위험해서
  추가했습니다. [설정] 그룹(준비·연결/테마/설정) 바깥에서 새로 들어올
  때만 물어보고, 그 안에서 하위 탭끼리 옮겨 다닐 때는 다시 안 묻습니다.

---

## v0.2.1

- **로그인 기능을 넣은 뒤 프로그램이 아예 실행이 안 되던 문제** — 서버는
  실제로는 정상적으로 떴는데(로그에 "Uvicorn running..."이 매번 찍혀
  있었습니다), 트레이 아이콘이 "서버가 떴는지" 확인할 때 쓰던
  `/api/setup`이 이제 로그인해야만 볼 수 있는 주소가 돼서 401을
  돌려받았고, 트레이는 이걸 "서버가 안 떴다"로 오판해 "서버를 시작하지
  못했습니다"라는 잘못된 오류를 띄웠습니다. 로그인 여부와 무관하게 항상
  응답하는 `/api/auth/check`로 헬스체크를 바꿔 고쳤습니다.

---

## v0.2.0

### 추가한 기능

- **화면 잠금 비밀번호(숫자 6자리)** — Tailscale 등으로 외부에서 접속할
  수 있게 되면서, 이 서버가 인증 없이 계좌를 조작할 수 있었던 문제를
  막기 위해 추가했습니다. 프로그램을 열면 비밀번호부터 물어보고,
  틀리면 어떤 조작도(주문·설정·자동매매 시작/정지 전부) 할 수 없습니다.
  기본 비밀번호는 **123456**이고, [준비·연결] 화면의 "④ 화면 잠금
  비밀번호" 카드에서 바꿀 수 있습니다 - 바깥에 열어 두셨다면 꼭 기본값을
  바꾸세요. 비밀번호를 바꾸면 다른 곳에서 로그인해 둔 세션은 전부
  자동으로 로그아웃됩니다.

### 고친 버그

- **어떤 매매 기법이 손실 없이 전부 승리하면 [성과]/[매매 규칙] 화면이
  통째로 빈 화면이 되던 크래시** — 손익비는 손실이 0건이면 수학적으로
  무한대인데, 이 값을 그대로 응답에 실었더니 서버의 JSON 응답 규칙과
  맞지 않아 관련 화면이 전부 500 오류로 죽었습니다(승률 100%인 기법이
  하나만 있어도 재현됩니다 - 이번 세션에서 여러 기법의 승률을 끌어올린
  뒤 실제로 처음 겪었습니다). 무한대는 화면에 "∞"로 표시하도록 안전하게
  고쳤습니다. 모바일에서 접속했을 때 일부 화면이 안 보인다는 문의의
  실제 원인이 이것이었을 가능성이 높습니다 - 화면 크기와는 무관한
  문제였습니다.

---

## v0.1.5

- **DAYTRADER_HOST 환경변수가 트레이(시스템 트레이) 실행 경로에서
  조용히 무시되던 문제** — 서버 자체는 이 환경변수로 바인딩 주소를 바꿀
  수 있다고 스스로 안내하는데(기본은 보안을 위해 127.0.0.1 만 허용,
  다른 값이면 경고만 남기고 그대로 엶), 트레이 아이콘이 실제 서버
  프로세스를 띄울 때는 이 값을 항상 "127.0.0.1"로 덮어써 버렸다.
  그래서 Tailscale 등으로 외부(다른 기기)에서 접속하려고
  `DAYTRADER_HOST=0.0.0.0`을 미리 설정해도 항상 localhost로만
  열려 있어 8000번 포트에 접속할 수 없었다. 이제 트레이도 이 환경변수를
  그대로 물려받아 적용한다.
  ⚠️ 이 서버에는 인증이 없습니다 - 0.0.0.0(또는 특정 인터페이스 IP)으로
  열면 그 네트워크에 있는 누구나 계좌를 조작할 수 있습니다. Tailscale처럼
  신뢰할 수 있는 사설망에서만 이렇게 여세요.

---

## v0.1.4

- **위 v1.1.3 크래시까지 고친 뒤 실제 보유 종목으로 재확인하다 드러난 세
  번째 크래시** — `NotOwnedError: 001440 은 이 브로커가 산 적이 없습니다`.
  "이 프로그램이 직접 사지 않은 종목은 절대 안 판다"는 핵심 안전장치가,
  재시작 전에 분명히 이 프로그램이 직접 사서 상태 파일에 남아 있는
  포지션까지 "산 적 없다"고 오판했습니다. 매수 장부(브로커 내부의
  `_bought_qty`)는 프로그램을 재시작할 때마다 텅 빈 채로 새로 만들어지는데,
  상태 파일에서 포지션을 불러오는 것과 별개라 서로 어긋나 있었습니다.
  이제 상태를 불러온 직후 그 수량만큼 매수 장부에도 자동으로 등록해,
  재시작 전에 산 종목을 재시작 후에도 정상적으로 청산할 수 있습니다.
  실제로 보유 중이던 종목(대한전선)으로 처음부터 끝까지 재현해 확인했습니다.

---

## v0.1.3

- **위 v1.1.2 크래시를 고친 뒤 바로 이어서 드러난 두 번째 크래시** —
  `TypeError: '>' not supported between instances of 'str' and 'int'`.
  토스 API가 현재가를 문자열로 줄 때가 있는데("31500"), 청산 관리
  로직이 그 값을 숫자로 바꾸지 않고 그대로 고점(peak_price)과 비교해서
  죽었습니다. 이 예외는 메인 루프에서 잡히지 않아 보유 종목이 있는 채로
  국내 자동매매를 시작하면 역시 곧바로 멈췄습니다. 시세를 받는 두 곳
  (매매 중 청산 관리, 재시작 직후 재개)에서 모두 숫자로 안전하게 바꾸도록
  고쳤고, 숫자로 바꿀 수 없는 값은 시세 없음과 같이 취급해 건너뜁니다.

---

## v0.1.2

- **보유 포지션이 있는 채로 국내 자동매매를 시작하면 즉시 죽던 크래시** —
  `AttributeError: 'str' object has no attribute 'tzinfo'`. 상태 파일에는
  매수 시각(`entry_time`)이 문자열로 저장되는데, 재시작할 때 그 문자열을
  그대로 포지션 객체에 넣고 있었습니다. 날짜 계산 함수가 datetime 객체를
  기대하고 문자열을 넣으면 즉시 예외가 나는데, 하필 재시작 직후
  `resume()`이 상태를 한 번 더 저장하는 순간 이 경로를 타서, 보유 종목이
  하나라도 있으면 국내 자동매매를 아예 켤 수 없었습니다. 불러올 때 다시
  datetime으로 되돌리도록 고쳤습니다.

---

## v0.1.1

- **국내주식 자동매매 중에 [설정] → 종목 선별에서 값(예: 최소 거래대금)을
  올려 저장해도 반영되지 않던 문제** — 두 가지가 겹쳐 있었습니다.
  1) 엔진은 시작할 때 받은 설정을 매매가 끝날 때까지 그대로 들고 있어서,
     실행 중에 설정을 저장해도 이미 떠 있는 엔진은 재선정("지금 갱신"
     버튼 포함)을 다시 돌려도 낡은 값을 계속 썼습니다. 손절·익절 같은
     위험 설정은 매매 중 바뀌면 위험할 수 있어 그대로 잠가 두되, 종목
     선정 기준(`screen.*`, 이미 산 포지션의 안전과 무관)은 재선정
     시점마다 설정 파일에서 최신 값을 다시 읽어오도록 고쳤습니다.
  2) 더 나쁜 문제 - 국내주식 거래 중에는 [설정] 화면이 종목 선별 입력칸
     자체는 계속 입력 가능한 상태로 놔둔 채, 저장을 누르면 그 값을 서버로
     보내기 직전에 조용히 지워 버리고도 항상 "저장했습니다"라고 떴습니다.
     실제로는 아무것도 안 바뀌었는데 사용자는 반영됐다고 믿을 수밖에
     없었습니다. 이제 잠긴 시장의 값이 실제로 바뀌었으면 "OO 거래 중이라
     해당 설정은 저장되지 않았습니다"라고 분명히 알리고, 나머지(잠기지
     않은) 설정만 저장합니다.

---

## v0.1.0

국내·해외·암호화폐 세 시장 모두 실제로 수익이 나는지 확인해 달라는 요청에
따라, 합성 시세로 4주(20거래일/168~672시간)치 모의매매를 빠르게 재현하는
진단 스크립트를 만들어 돌려봤습니다. 그 과정에서 찾은 구조적 문제들을
고쳤습니다. (합성 시세 기반 백테스트라 실제 시장 수익을 보장하지는
않습니다 - 아래 "참고" 참조.)

### 고친 버그

- **모멘텀 소멸(momentum_fade) 청산의 "거래량 위축" 판정이 단위가 완전히
  달라 사실상 늘 통과로 오판되던 버그** — 매수 시점의 봉 하나 체결
  거래량과 비교해야 하는데, 실제로는 테마 스크리닝 때 구한 종목의 당일
  누적 거래대금(원 단위·수십억)을 넣고 있었습니다. 단위가 하늘과 땅
  차이라 "거래량이 줄었다"는 조건이 사실상 항상 참으로 판정됐고, 그
  결과 연속 하락 3봉·VWAP 이탈 중 아무거나 하나만 겹쳐도 청산되는 셈이
  되어 이 기법이 다른 모든 청산 기법보다 먼저, 너무 이르게 발동했습니다
  (4주 백테스트에서 국내주식 청산의 100%가 이 기법으로만 나갔고, 그중
  승률은 10~16%에 불과했습니다). 매수 시점 봉의 실제 거래량을 넣도록
  고쳤습니다.
- **국내주식·해외주식 기본 청산 기법에서 momentum_fade 를 제거** — 위
  버그를 고친 뒤에도 momentum_fade 로 청산된 거래만 따로 보면 승률이
  여전히 10~16%로 나빴습니다(시간손절 88~100%, 강제청산 100%와 대조적).
  이익을 지키려던 기법이 실제로는 시간손절이 이익을 키울 기회를 매번
  미리 잘라먹고 있었던 셈이라, 국내·해외 기본값에서 뺐습니다. 4주
  백테스트 기준 국내주식 수익률이 -12.7%에서 +5.0%로, 승률이 5%에서
  86%로 바뀌었습니다. (암호화폐는 사정이 달라 그대로 둡니다 - 코인
  포지션은 애초 이 버그의 영향을 받지 않았고, 백테스트에서 momentum_fade
  를 빼면 오히려 성과가 나빠졌습니다.)
- **해외주식 종목당 매수금액이 감시종목 전체 개수로 나뉘어 있던 문제** —
  실제로 동시에 15종목을 다 사는 일은 거의 없는데도(1~2종목이 대부분)
  자금을 항상 관심종목 15개로 나눠 버려서 종목당 배정이 약 6만원(약
  $43)에 불과했습니다. 승률이 괜찮아도 건당 손익이 통계적으로 무의미할
  만큼 작아 수익을 낼 수 없었습니다. 국내주식의 "동시 보유 한도"와 같은
  개념(`overseas.max_positions`, 기본 4종목)을 추가해 실제로 동시에 들고
  갈 만큼만 나누도록 고치고, [설정] → 해외주식에 "동시 보유 한도(종목)"
  필드로 노출했습니다. 4주 백테스트 기준 총 손익이 $6.71 → $81.73,
  승률이 38% → 48%로 개선됐습니다.

### 참고 - 이번 백테스트에 대해

- 진짜 API 대신 규칙 기반 랜덤워크로 만든 합성 시세로 국내 4주·해외
  4주·코인 2주를 빠르게 재현해 진단했습니다. 실제 시장의 통계적 특성과
  다를 수 있어, 여기서 나온 승률·손익 숫자가 실제 운용 결과를 보장하지
  않습니다. 다만 "어떤 청산 기법이 다른 기법보다 먼저 발동하는가", "종목당
  배정금액이 시장 구조상 얼마나 되는가" 같은 구조적 문제는 시세가
  합성이어도 그대로 드러나며, 이번에 찾은 문제들은 전부 이런 종류입니다.
- 어떤 시장이든 무조건 수익을 보장하는 매매 기법은 없습니다 - 위 개선은
  "구조적으로 손해 볼 수밖에 없던 지점"을 없앤 것이지, 실제 시장에서도
  항상 이긴다는 뜻은 아닙니다. 실거래 전에는 모의매매로 충분히 지켜보는
  것을 권합니다.

---

## v0.0.1

- **장 마감 후 국내 자동매매를 켜면 아무 것도 안 하고 바로 꺼지던 문제** —
  보유 종목이 없는 상태로 장 마감 후 시각에 시작하면 "오늘은 더 할 매매가
  없다"며 곧바로 종료됐습니다. 사용자 입장에선 자동매매 버튼을 눌러도
  켜지자마자 꺼지는 것처럼 보였습니다. 이제 국내 자동매매는(모의·실거래
  공통) 장 마감 후에 시작해도 종료되지 않고 대기하다가, 다음 개장 시각이
  되면 스스로 종목 선정과 매매를 시작합니다. [종목 선정] 화면에는 기존과
  같은 방식으로 "지금은 종목을 선정하지 않습니다 · 다음 진입 시작: OO"
  안내가 표시됩니다. 시뮬레이션(sim)·리플레이 모드는 정해진 하루치만
  재생하는 백테스트이므로 예전처럼 하루가 끝나면 정상 종료됩니다(대기
  루프에 들어가지 않습니다).

---

## v0.0.0

6시간 모의매매 결과를 함께 진단하면서 발견한 문제들을 고친, 첫 정식 버전입니다.

### 고친 버그

- **암호화폐 청산이 코인 전용 설정을 무시하던 버그** — 손절·익절·트레일링·최대보유시간이
  실제로는 항상 국내주식 값(2.5%/5%/90분)으로만 판정되고 있었습니다. [설정] → 암호화폐에서
  정한 값(5%/10%/24시간)이 화면에만 보이고 실제 매매엔 전혀 반영되지 않던 것을 고쳤습니다.
- **해외주식 과매매 방지 장치 부재** — 손절 후 재진입을 막는 쿨다운, 일일 손실 한도,
  연속 손절 중단이 국내주식 엔진에는 있는데 해외주식 엔진에는 아예 없었습니다. 6시간
  세션에서 같은 종목이 39번 거래된 원인이었습니다. 국내주식과 같은 개념으로 추가했습니다.
- **해외주식 진입 판단이 일봉 기준이던 문제** — 청산은 실시간가로 타이트하게 관리하면서
  진입 신호는 일봉(하루 종일 안 바뀜)으로 판단해, 손절 직후 같은 신호로 곧바로 재매수되는
  일이 반복됐습니다. 진입 판단도 분봉 기준으로 바꿔 청산과 같은 시간 단위를 쓰게 했습니다.
- **해외주식 자동선정에 품질 필터 없음** — 그날 가장 많이 움직인 초저가 종목(예:
  $0.1~0.2대)이 뽑혀 고정 손절·익절과 상성이 나빴습니다. 최소 매수 가격, 당일 과열 제외
  기준을 추가했습니다.
- **해외·코인 모의매매에 거래 수수료 미반영** — 실제보다 성과가 좋게 보였습니다. 매수
  가능 여부 판정은 그대로 두고, 매도 시점에 왕복 수수료를 정산하도록 반영했습니다.
- **국내주식 종목선정이 항상 실패하던 버그** — 토스 랭킹 API가 가격 정보를
  `price.lastPrice`/`price.changeRate`처럼 한 단계 감싸서 주는데, 코드가 최상위
  `changeRate`를 읽고 있어서 모든 종목의 등락률이 항상 0%로 계산됐습니다. "테마로 인정"
  기준(2종목 이상 +3% 동반 상승)을 어떤 종목도 통과할 수 없었던 진짜 원인이었습니다.
- **랭킹 API 오류가 조용히 빈 결과로 둔갑하던 문제** — 요청 한도 초과 등으로 응답이
  200 OK인데 알 수 없는 모양이면 그냥 빈 목록으로 처리돼, 원인을 로그에서도 확인할 수
  없었습니다. 이제 그 응답 내용을 로그에 남깁니다.
- **VWAP 눌림목 진입 기법의 부등호가 반대로 되어 있던 버그** — "직전봉보다 낮은 저가를
  찍고 양봉으로 복귀"를 확인해야 하는데, 정반대로 저가를 갱신하지 **않은** 경우에
  통과하고 있었습니다.
- **ATR 변동성 손절이 상태 손상 시 죽는 버그** — `entry_price`가 없을 때 예외가 나는
  결함이 있었는데, 지금까지 이 기법이 어디서도 기본으로 켜진 적이 없어 드러나지
  않았습니다. 이번에 기본값으로 켜면서 함께 고쳤습니다.
- **재빌드하면 API 키·설정·매매기록이 통째로 초기화되던 문제** — 처음엔
  `config.yaml`만 덮어쓰지 않게 고쳤는데, 알고 보니 PyInstaller가 `--noconfirm`
  옵션으로 `dist/AutoDayTrading` 폴더 **전체를 지우고 새로 만들기** 때문에
  `secrets.yaml`(API 키)·`state/`(매매 기록)·`logs/`까지 함께 사라지고 있었습니다.
  이제 빌드 시작 전에 이 파일들을 임시 폴더로 백업해 뒀다가, 빌드가 끝난 뒤 그대로
  되돌려놓습니다. 실제 PyInstaller 삭제·재생성 과정을 그대로 흉내낸 시나리오로
  API 키·설정·모의매매 기록·실거래 기록이 전부 살아남는 것을 확인했습니다.
- **실제로 시세를 받아온 적 없는 "야간시장" 항목 삭제** — [시장] 화면 맨 아래 HLKR
  참고용 그룹은 공식 API가 없어 항상 링크 안내만 보여주고 있었습니다(모든 값이 항상
  비어 있음). 실제로 동작하는 미국 지수선물 그룹은 그대로 유지했습니다.

### 추가한 기능

- 코인·해외주식 대시보드에 **성과 요약 패널**(승률·손익비·평균 이익/손실·기법별 성과·
  과다거래 경고 배지) 추가.
- 해외주식에도 코인처럼 "신규 매수 중단 중 · 재개 예정 시각" 배너 추가.
- [종목 선정] 화면에 **"↻ 지금 갱신" 버튼** 추가 — 예전엔 정해진 주기(기본 30분)가
  될 때까지 기다리는 것 말고는 다시 선정할 방법이 없었습니다.
- [시장] 화면의 기준 시각·출처를 상단 한 줄로 통합 — 카드마다 반복해서 찍던 출처
  표기를, 전체 기준과 다를 때만 보이도록 정리.
- **손실을 줄이는 청산 기법을 기본값으로 활성화** — `atr_stop`(ATR 변동성 손절)과
  `momentum_fade`(모멘텀 소멸 청산)는 이미 구현돼 있었지만 국내·해외·코인 어디서도
  기본으로 켜진 적이 없었습니다. 둘 다 국내주식·해외주식·암호화폐 전부 기본값에
  추가했습니다.
- **코인 전용 추세 필터 기간 조정** — `ma_pullback`/`rsi_pullback`의 추세 필터가 20분
  기준이었는데, 코인은 최대 24시간을 보유할 수 있어 추세가 아니라 잡음을 보고
  있었습니다. 코인만 60분 기준을 쓰도록 시장별 파라미터 오버라이드를 추가했습니다.
- [설정] 화면에 새 필드 노출 — 코인 거래 수수료율, 해외주식 최소 매수가·과열 제외 기준.
- **암호화폐·해외주식 실거래도 "계좌가 진실이다" 원칙으로 대조하도록 추가** — 국내주식
  엔진에는 프로그램 재시작 시 실제 계좌와 로컬 매매 기록을 대조해, 프로그램이 모르는 새
  프로그램 밖에서 청산된 "유령 포지션"을 정리하는 로직이 있었는데 코인·해외주식 엔진에는
  없었습니다. 실거래(live) 모드로 시작할 때 빗썸 잔고/토스 보유 종목을 조회해, 로컬에
  남아있는 포지션의 실제 잔고가 크게 부족하면(10% 미만) 그 사이 프로그램 밖에서 청산된
  것으로 보고 그 시점 시세로 손익을 추정해 매매 기록에 남기고 포지션을 정리합니다.
- 버전 표기 도입 — 대시보드 상단(연결 상태 옆)과 `AutoDayTrading.exe --check`에 지금
  실행 중인 버전을 표시합니다. 재빌드 후 실제로 반영됐는지 화면에서 바로 확인할 수
  있습니다.

### 참고

- 이번 버전부터 코인은 `entry_params`(시장별 기법 파라미터 오버라이드)라는 새 구조를
  씁니다 — 국내주식과 같은 기법을 쓰되 파라미터만 시장에 맞게 다르게 줄 수 있습니다.
- exe로 실행 중이던 프로그램은 이 버전들을 받으려면 재시작(`python run.py ui`) 또는
  재빌드가 필요합니다. config.yaml 값만 바뀐 항목은 설정을 다시 저장하면 바로
  반영됩니다.
