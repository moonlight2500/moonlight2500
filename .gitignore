# ★★★ API 키는 secrets.yaml 한 곳에만 있다 - 이 파일만 빼면 안전하다.
#   (예전에는 .env 와 config.yaml 에 흩어져 있어 실수로 올리기 쉬웠다)
secrets.yaml
.env
.token_cache.json

# 실행 중 생기는 상태·기록
state/
*.log
__pycache__/
*.pyc

# 빌드 산출물
build/
dist/
*.spec

# 리서치 백테스트 - 내려받은 시세 캐시·생성된 보고서는 올리지 않는다(코드만).
research/cache/
research/out/
